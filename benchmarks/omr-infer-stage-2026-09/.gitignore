# The post-INFER record is ~133 MB and is a build product: reproduce it with
# `bash ../omr-cleanup-count-2026-09/run_gather.sh 1-4 p1-p4` then
# `reinfer.py <record> --out out/record-inferred.json`.
out/record-inferred.json
out/record-*.json
