── baseline ──
   green

  RED     a forward counts as a supply
  RED     positional arguments are ignored
  RED     the fixpoint runs one pass only
  RED     a splat counts as a supply
  RED     subject=/scope= reads are judged as EXACT
  SURVIVED an unresolvable Kind defaults to a real one  ⚠️ the test does not reach this
  RED     the gather walk skips its interprocedural round
  RED     a TEST naming a key counts as reading it
  RED     this module's own gap list counts as reading
  RED     the row-kwarg list is hand-written
  RED     a dead question exits like an ordinary finding
  RED     ⚠️ THE FRAME FAULT RESTORED: read the roster at EXACT
  RED     the veto names nothing it looked at
  RED     a PAGE-sourced roster is admitted
  RED     the record carries the object, not a dict
  RED     the CLI has the flag and does not pass it
  RED     --no-roster is ignored
  RED     roundtrip compares KEY NAMES, so a rename reads as a drop
  RED     roundtrip skips fields nothing reads, so the live one is lost
  RED     a dropped field's READ flag is always False
  RED     --run looks for the rows under the wrong key
  RED     --run reports zero instead of refusing
  GREEN   POSITIVE CONTROL: a no-op edit must stay GREEN

── 21 red, 2 survived, 0 errors
   SURVIVED: an unresolvable Kind defaults to a real one
   SURVIVED: POSITIVE CONTROL: a no-op edit must stay GREEN
