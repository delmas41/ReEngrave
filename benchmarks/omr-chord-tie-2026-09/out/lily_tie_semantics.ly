\version "2.24.0"
% Does a CHORD-LEVEL `~` resolve against the following chord by PITCH?
% Bar 1: three notes tied to a chord holding only two of them.
% Bar 2: a chord tied to a chord sharing NO pitch.
{ <c' e' g'>4~ <c' e'>4 <c' e' g'>4~ <d' f' a'>4 }
