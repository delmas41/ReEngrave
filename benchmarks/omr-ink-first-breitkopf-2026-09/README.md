# The ink-first test on the SHATTERING plate

Lane D, 2026-09-22. Closing the named blocking artefact of
`docs/breakthrough-2026-09-18-the-unit-of-enquiry.md` §7: a Breitkopf record
with `OMR_INK` on, over the four pages the crop pass adjudicated, and then the
ink-first test with a residue split and a print join.

WORK IN PROGRESS — committed at minute zero so the gather it launches is
stamped against a CLEAN tree (`provenance.dirty` is true for anything edited
mid-run).
