# The staged records are 2-10 MB each and are BUILD PRODUCTS: `run_arms.py`
# reproduces them from the fixture and the weights. What is committed is the
# reduction — `*.meter.json` and `REPORT.txt` — which is what every claim in
# FINDINGS.md is read off.
out/*.json
!out/*.meter.json
out/*.musicxml
out/*.err
out/*.log
# ⚠️ `out/*.png` alone does NOT match a SUBDIRECTORY, and 41 debug crops
# reached a commit through that gap. Match at any depth.
out/**/*.png
out/*.png
# LilyPond renders, regenerated by `render_boundary.py` in ~15 s each.
fixtures/
