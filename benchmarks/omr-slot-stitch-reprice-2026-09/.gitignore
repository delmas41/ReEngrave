# Derived truths are BUILD PRODUCTS of page_normalise.py — regenerated by
# reprice_arm.py on every run, and their raw sha256 is not reproducible across
# days (music21 stamps a fresh <encoding-date> and random instrument ids).
# See structural-floor-measured.json's ⚠️_committed_control_hash_is_date_dependent.
derived-truth/
