# The staged pipeline's 21 decisions

Generated by `python3 -m tools.omr.staged.inventory` — every column derived from the code, none typed.

| # | quantity | decision | scope | domain | mode | state | consumes | produces → | legacy named | checkable |
|--:|---|---|---|---|---|---|---|---|---|---|
| 0 | `system_membership` | `adjudicate_system_membership` | system | all | additive | real | system_staff_count*, gap_bridging | — | — | mixed |
| 1 | `staff_group` | `adjudicate_staff_group` | staff | all | additive | real | bracket_block | group_symbol, instrument | — | mixed |
| 2 | `measure_partition` | `adjudicate_measure_partition` | staff | all | additive | real | barline_column | — | — | mixed |
| 3 | `staff_ordinal` | `adjudicate_staff_ordinal` | staff | all | additive | real | staff_ordinal* | instrument, part_partition, slot_index | — | uncheckable |
| 4 | `system_staff_count` | `adjudicate_system_staff_count` | system | all | additive | real | system_staff_count* | group_symbol, part_partition, slot_index, system_membership | — | mixed |
| 5 | `instrument` | `adjudicate_instrument` | staff | all | additive | real | margin_label, roster_entry, staff_ordinal*, staff_group* | clef, glyph_owner, group_symbol, part_partition, slot_index, part_name | clef_correction, contextual, instruments | mixed |
| 6 | `slot_index` | `adjudicate_slot_index` | staff | all | additive | real | instrument*, staff_ordinal*, system_staff_count* | part_partition | slots | mixed |
| 7 | `part_partition` | `adjudicate_part_partition` | document | all | additive | real | slot_index*, staff_ordinal*, system_staff_count*, instrument* | part_name | export | mixed |
| 8 | `group_symbol` | `adjudicate_group_symbol` | system | all | additive | real | staff_group*, instrument*, system_staff_count* | — | export | uncheckable |
| 9 | `clef` | `adjudicate_clef` | staff | all | competitive | real | clef_glyph, clef_position, clef_located, clef_seed, notehead_staff_position, instrument*, keysig_clef_fit | glyph_owner, key_signature, pitch | clef_correction | mixed |
| 10 | `key_signature` | `adjudicate_key_signature` | staff | all | additive | real | keysig_run_position, keysig_marker, keysig_clef_fit, clef*, dossier_fact | accidental | key_signature_locator | mixed |
| 11 | `glyph_owner` | `adjudicate_glyph_owner` | glyph | glyph_band_distance | additive | real | glyph_ladder, glyph_band_distance, glyph_conf, notehead_staff_position, instrument*, clef* | arc_owner, dynamic, pitch | — | mixed |
| 12 | `arc_owner` | `adjudicate_arc_owner` | glyph | arc_box | additive | STUB | arc_box, notehead_staff_position, glyph_owner* | — | — | uncheckable |
| 13 | `arc_kind` | `adjudicate_arc_kind` | glyph | arc_box | additive | STUB | arc_box, notehead_staff_position | — | — | mixed |
| 14 | `articulation_owner` | `adjudicate_articulation_owner` | glyph | articulation_mark | additive | STUB | articulation_mark, glyph_box | — | — | uncheckable |
| 15 | `wedge_anchor` | `adjudicate_wedge_anchor` | glyph | wedge_box | additive | STUB | wedge_box, glyph_box | — | — | uncheckable |
| 16 | `tuplet_ratio` | `adjudicate_tuplet` | cell | tuplet_marker | additive | real | tuplet_marker, beam_stroke, notehead_class | duration | — | mixed |
| 17 | `duration` | `adjudicate_duration` | glyph | notehead_class, rest | additive | real | beam_stroke, flag, aug_dot, notehead_class, stem, tuplet_ratio*, glyph_box, rest | meter | — | mixed |
| 18 | `meter` | `adjudicate_meter` | system | all | additive | real | meter_glyph, meter_template, duration*, dossier_fact | duration, duration | — | mixed |
| 19 | `dynamic` | `adjudicate_dynamic` | cell | dynamic_letter | additive | STUB | dynamic_letter, glyph_owner* | — | export | uncheckable |
| 20 | `direction` | `adjudicate_direction` | cell | all | additive | STUB | direction_word | — | — | uncheckable |

`*` = also produced as a VERDICT by another decision; `!` = **never gathered and never decided — this input does not exist in the log at all**; everything else is a MEASUREMENT gathered off the page.

## Consequences (EVALUATE)

| consequence | cause → effect | scope | state |
|---|---|---|---|
| `restate_pitch` | `clef` → `pitch` | staff | real |
| `size_measure_rest` | `meter` → `duration` | cell | real |
| `reconcile_duration` | `meter` → `duration` | cell | real |
| `move_glyph` | `glyph_owner` → `pitch` | glyph | real |
| `respell_accidental` | `key_signature` → `accidental` | staff | real |
| `name_part` | `instrument` → `part_name` | staff | real |
| `join_parts` | `part_partition` → `part_name` | document | STUB |

## Declared stubs: 6

`arc_owner`, `arc_kind`, `articulation_owner`, `wedge_anchor`, `dynamic`, `direction`

## ⚠️ PROBLEMS — 17, 0 of them NOT on `KNOWN_GAPS`

`KNOWN_GAPS` is an INVENTORY, not a suppression list: every standing gap with its reason. `--check` fails on anything not on it, **and on an entry nothing reports any more** — a closed gap must LEAVE the list or it stops describing the pipeline and starts describing its history.

- system_membership declares 'gap_bridging' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration. The connectivity veto already ran in GATHER and `adjudicate_system_membership` records its result, so the decision reads the count and not the bridging.
- instrument declares 'roster_entry' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration; the roster tier is not wired into the staged identity decision yet (`OMR_ROSTER_LABELS` is off on the legacy path too).
- instrument declares 'staff_ordinal' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration.
- instrument declares 'staff_group' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration.
- part_partition declares 'staff_ordinal' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration.
- part_partition declares 'instrument' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration; the join is decided from staff COUNTS and slots.
- group_symbol declares 'system_staff_count' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration; the symbol is decided from the groups and the instruments, deliberately NOT from the staff count -- which is the incumbent rule it exists to replace.
- clef declares 'notehead_staff_position' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* ⚠️ NOT MERELY INERT: this quantity is the clef's OWN FIRST `checked_by` entry (implied pitches against the instrument's written range) and the body never reads it, so a declared constraint names a check the code does not run. ⚠️⚠️ AND IT CANNOT BE WIRED ON A SCAN, MEASURED 2026-09-09: the range test needs the INSTRUMENT, which abstains `no_evidence` on 22 of 22 and 27 of 27 staves of the two scanned pages -- so it would reach NONE of the population it was filed to fix. What did reach it is `Q.CLEF_POSITION`, which needs no identity. The declaration stays because the check is real where a dossier or a margin label exists; what changed is that nobody should reach for it as the fix for an abstaining clef on a scan.
- key_signature declares 'keysig_marker' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration; the decision reads `keysig_clef_fit`, which the markers already feed in GATHER.
- key_signature declares 'dossier_fact' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration; no dossier is supplied on the scan path by protocol.
- glyph_owner declares 'glyph_conf' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* ⚠️ THE STANDING OBSERVATION, reproduced: `_dedupe_cross_staff_detections` has both detections' confidences in hand at the moment it decides and uses neither. The staged decision declares it and still does not read it.
- glyph_owner declares 'notehead_staff_position' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* ⚠️ MISDIRECTED rather than inert: `_range_veto` does read a staff position, out of `band_row.detail['position_in_candidate']`, so `wants` names the wrong quantity for a real dependency.
- tuplet_ratio declares 'beam_stroke' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration. The legacy rule takes a tuplet group's EXTENT from the beam box (`rhythm._beamed_groups`); the staged decision groups by 'exactly as many heads as the digit claims' in the cell instead, so the beam is declared and unused.
- duration declares 'stem' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration. `adjudicate_duration` DOES read `beam_stroke` -- both come from `gather_cv_lines` -- but nothing reads the stem: no stem-direction or stem-presence tier exists on this path.
- meter declares 'meter_glyph' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* ⚠️ inert, and the DETECTOR's meter reading is what is unread. The vote runs on `meter_template` alone, so a `timeSigCommon` detection -- which the legacy exporter uses for `symbol=` -- reaches no decision. The staged exporter derives `symbol` from the template reader's own matched letter instead, so nothing is lost today; a second witness is.
- meter declares 'duration' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* declared and deliberately not read: the bar-sum check is this decision's strongest constraint and `adjudicate_meter`'s own docstring says it belongs in EVALUATE as a bounded repair (`consequences.reconcile_duration`), not in the vote.
- meter declares 'dossier_fact' in `wants` and never reads it — the declaration is inert: nothing records it as missing or declined
  - *known:* inert declaration; no dossier is supplied on the scan path by protocol.
