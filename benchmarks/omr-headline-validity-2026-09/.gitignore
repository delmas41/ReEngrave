# Build products. Every one is regenerated by the probe that wrote it, and the
# derived truths carry a .provenance.json naming their source, the transform
# version and the merge map — so committing them would freeze a copy of
# something that must be re-derived when the transform changes.
derived-truth/
derived-truth-engraved/
live-run/
