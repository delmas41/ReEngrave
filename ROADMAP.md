# ROADMAP — from here to a finished score

The phases and gates are defined in
[docs/plan-2026-09-22-from-here-to-a-finished-score.md](docs/plan-2026-09-22-from-here-to-a-finished-score.md).
This file is the STATUS of each item and nothing else. A session that takes
an item puts its branch beside it; a session that finishes one changes its
status and names the commit. Nobody writes a handoff — the line here and the
benchmark `FINDINGS.md` are the record.

Status words: `todo` · `in flight (branch)` · `done (sha)` · `blocked (why)`
· `dropped (why, DECISIONS line)`.

**Definition of done for the product** (plan §4): one command, a whole
movement, MusicXML + LilyPond PDF, unread bars marked never invented, every
staff named or held out, and a cleanup count Sean would rather pay than
re-enter.

---

## START HERE — state at the end of the session of 2026-09-23

Main is `16ed0f50`; branch `claude/start-here-questions-7d5637` carries Sean's four answers below (`64c83c5f`, `fd656c20`, `6f269360` and the roadmap commit). The tree is the record; this
block is the pointer. Read CLAUDE.md first (short), then this file, then
`docs/DECISIONS.md`.

**Landed today, in order:** **1.2** (the decide-stage cost lane, verified and
landed — see its two gate corrections below) · **1.2b opened** (the budget
line, the second of 1.2's two defects, still untouched) · **1.4** (a first
cleanup count on the Beethoven page — **not Sean's**) · **1.1 Brahms whole
movement** · **2.3** (evidence cut, the defect found and fixed).

**The measurement of the day: 1.2 works at scale.** Brahms 1 mvt 1, 27 denser
Breitkopf pages, **1 h 11 m** — GATHER ~28 min, ADJUDICATE ~33 min (28
decisions, 196,757 verdicts), GROUPS+EVALUATE ~5 min. Against Beethoven's 16
pages at **12.8 h**. EVALUATE took 4 m 55 s, so **1.2's named-but-unfixed
residual did not bite**. ⚠️ Not a controlled base-vs-arm — different document
and page count — but consistent with 1.2's own superlinear claim.

**Acceptance now covers all three** (`current.json` previously held only
Beethoven): Litolff notes 7,878/11,632 (0.677); Breitkopf 13,952/24,533
(0.569); engraved 358/371 (0.965), F1 0.947, OMR-NED 0.1519. ⚠️ **Breitkopf's
0.569 against the old 4-page 0.678 is NOT a regression** — that record predates
2.4a, whose two refusals hold back 4,043 glyphs (16.5% of gathered); excluding
them gives 0.681. Do not quote the pair without the decomposition.

**Two gate corrections 1.2 needed before it could land**, both worth knowing
because the lane's own report asserted otherwise:
- its identical-verdict control **could not finish on base** because the
  control adjudicated all 7 systems and only filtered to one when dumping.
  Restricted to one system's WORK it runs: **0 of 429 differing** on
  subject/quantity/outcome/value/reason/basis/considered. The 52 `correlated`
  differences are all the documented singleton removal, classified one by one.
- one of the rewrite's **stated proofs was false** — the bucket-representative
  shortcut assumed `_one_signal` ignores which observation it compares against;
  it does not, through `closure(b)`. Repaired with a strictly faster rule.

**Sean answered the four open questions on 2026-09-23** (in one message,
after the block above was written); the tree now carries the answers:
1. **2.3 DECIDED: both duration rules ON** (`6f269360`). Measured OFF→ON on
   both whole-movement records, balanced, and the funnel inverts between the
   plates. Breitkopf has no subject checked against its own print yet.
2. **2.6 opened — `glyph_owner`'s domain** (`64c83c5f`, FINDINGS §10): all
   161 lack a band-distance row because GATHER's contest test compares smufl
   NAMES (which encode on-line vs in-space) and IoU 0.5 where the frozen
   legacy contest used category and 0.3. Process designed; a GATHER change,
   two re-gathers to price; 87 of 161 would enter, the 38 with no twin must
   not.
3. **2.7 opened — the in-bar accidental.** GATHER a position, ADJUDICATE the
   owner (right-of, same height, may abstain), EVALUATE a bar-scoped override
   of the key, EXPORT through the legacy renderer already reused. **One
   question for Sean before code**: how does Litolff print courtesy
   accidentals, and are they written with `parentheses="yes"` or suppressed?
4. **1.4 set up** (`fd656c20`): three readable per-system pages and three
   empty sheets; `counts/HOW-TO-COUNT.md` is what Sean opens first. His count
   is the next thing only he can do.

**Sean looked at the count pages (2026-09-23, later) and the count was
redirected before a number was written**: *"the amount of fixes is still too
many to count — we are not even close to getting the noteheads correct"*, *"it
is allowing for math that doesn't add up at all"*, *"the key signatures don't
make sense either — they don't match each other on the same page, even the
engraved Beethoven"*. All three measured and true (2.8, 2.9, and the Viola
funnel: 24 heads written where the print has 49). The count is NOT taken on
a page most of whose bars would be re-entered; those three sentences are the
count result and are in DECISIONS.

**Next actions, in order:**
1. 2.8 (hold out bars that do not add up) and 2.9 (key by system majority) —
   both cheap, both decision-level, both with a control that can fail.
2. Viola, Litolff p3 system 1 — **TRACED** (`d8c1b801`,
   `benchmarks/omr-notehead-funnel-2026-09/`): the detector boxed **48**
   heads on that staff and the file holds **0**; 40 refused `no_pitch`
   because the staff's CLEF abstained (`no_candidates`: no clef-class box —
   the alto clef is merged into the lines and was boxed as two noteheads),
   so `restate_pitch` had nothing and every head died at export. At most 2
   heads were never boxed; 0 went to a neighbour. The same part one system
   down (alto clef read) writes 24 of 27. **All 40 `no_pitch` refusals on the
   page are this one staff.** `slot_index` DID decide the staff is Viola. Next
   item (2.10, needs Sean's one line): a staff whose clef is unread and whose
   INSTRUMENT is decided takes the instrument's conventional header clef,
   GAPS ONLY, in INFER, labelled — the fact sheet already speaks a supplied
   clef gaps-only (§8), and identity-upstream is Sean's 09-05 inversion.
   Also found: the file's bar numbers run one behind the print from bar 48
   (the p2 dropped barline `works.json` records), and system 1's Viola
   writes 24 against the reference's 18.
3. 2.6 build (in flight, `claude/owner-domain-2.6`).
4. 2.7 (accidental) — PARKED — largest thing the count found; GATHER change, so plan
   its two re-gathers together with 2.6's.
3. 2.6 (`glyph_owner` domain) — build it as designed in FINDINGS §10, reach
   first on both scans.
4. A Breitkopf print check of the duration rules (2.3 is decided; this is
   evidence, not a re-decision): cut crops for a sample of the 287 inferences
   with `probe/crop_inferred.py` and send Sean the crops.
5. **1.2b** — the run script's budget still covers GATHER only; it printed
   "~41.9 min" for a run that took 1 h 11 m. Two data points now exist.
6. 3.1b, 0.2b, 0.4a/0.4d as before.

**Operational facts from the day:**
- **`infer.py` imports `inferences` LAZILY** (`_ensure_rules`), so editing that
  file mid-gather reaches a running process and makes its provenance stamp a
  lie. Work in a separate worktree until the run is past INFER.
- **`benchmarks/acceptance/out/` is deliberately NOT gitignored** (the
  clean-tree guard exempts it by path), so `git add -A` there sweeps in the
  gather's own 300-500 MB record and the push is rejected. Records live in
  `library/_shared-records/`; only derived artefacts are committed. Now
  gitignored by pattern.
- **A crop that does not name its staff is not evidence.** Sean, on a crop
  centred in the gap between two staves: *"i dont know which staff the cell is
  focussing on."* Crops now draw the filed staff's own lines.
- The gather script refuses to guess a movement's last page, and it is right
  to: `0-26` for Brahms was a ledger claim until the plate was checked.

---

## Phase 0 — Stop and consolidate

| item | what | status |
|---|---|---|
| 0.1 | Staged is the product path; legacy frozen | done (DECISIONS 2026-09-22) |
| 0.2 | Flag triage table, ≤ 15 product flags | done — table with a verdict on all 60 (`docs/flags-2026-09.md`) |
| 0.2b | Enforce the triage: remove the `promote — now` flags, delete `OMR_ADJUDICATE`, add `OMR_RESEARCH`, a derived test that staged reads no frozen flag and every `OMR_*` read has a row | todo |
| 0.3a | Archive CLAUDE.md → `docs/chronicle-2026-09.md` | done (2b441cb3) |
| 0.3b | New CLAUDE.md spec ≤ 6,000 words | done (2b441cb3; 4,700 words) |
| 0.3c | `docs/DECISIONS.md`, `ROADMAP.md` | done |
| 0.4a | Shared staged test fixture module; delete the four `_log` copies | todo |
| 0.4b | One `python3 -m tools.omr.staged.check` with `open-findings.json` | done — **baseline 250 open findings on 2026-09-22** (wiring 66, reach 26, gather_coverage 19, capture 18, inventory 11, brakes 8, trace 3, source-text tests 47 files, live mutation batteries 52) |
| 0.4c | Fast / slow test tiers derived from measured durations | done — full suite 4,806 passed / 20 skipped in 700 s; fast tier 2,695 tests in 75 s wall (`pytest -m "not slow"`); threshold derived from `tools/omr/tests/durations.json` |
| 0.4d | Archive the 52 mutation batteries under `benchmarks/_archive/`; no new ones | todo — `check` counts live ones meanwhile |
| 0.4e | No new source-text tests; `check` counts them | done — 47 files counted, allowlist of 2 |
| 0.5 | Lane discipline: brief from the tree, phase item, gate, print check | done (plan §6, rules 1–10) |

Gate: spec < 6,000 words; `check` writes `open-findings.json`; flag table
has a verdict on every flag; fast tier under two minutes.

## Phase 1 — The acceptance harness

| item | what | status |
|---|---|---|
| 1.1 | Whole-movement staged records for the three acceptance documents | **BOTH SCANS DONE.** **Beethoven 5 mvt 1 / Litolff**, pages 1–16, gathered on `6b7dd3a3`, **12.8 h** (GATHER ~25 min then ~12.3 h single-core in the deciding stages — the cost, not the reading, was the budget; see 1.2). **Brahms 1 mvt 1 / Breitkopf, pages 0–26 DONE 2026-09-23** on `275641c0`, clean tree, **1 h 11 m** — GATHER ~28 min, ADJUDICATE ~33 min (28 decisions, 196,757 verdicts), GROUPS+EVALUATE ~5 min, INFER+EXPORT the rest. ⚠️ **The movement boundary was VERIFIED against the plate, not assumed**: pdf page 27 opens *Andante sostenuto*, so movement 1 ends at page 26 and `--pages 0-26` is right. Record 456 MB = **16.9 MB/page**, inside 1.1b's 20 MB/page target for Breitkopf. **14 parts, 0 unidentified, not fragmented.** `library/_shared-records/brahms1-breitkopf-mvt1-whole-20260923.record.json`; the manifest points at it with its md5 receipt and carries the caveats. This is the first whole-movement run after 1.2 bounded the deciding stages, and it is the measurement that lane was for: **27 denser pages in 1 h 11 m against 16 pages in 12.8 h.** ⚠️ Not a controlled base-vs-arm — different document, different page count — but consistent with 1.2's own claim that the pre-fix cost was superlinear in system glyph count. EVALUATE took **4 m 55 s**, so **1.2's named-but-unfixed residual** (`size_measure_rest` rebuilding the `Q.DURATION` index once per cell) **did not bite at this scale**. Engraved: existing committed record. |
| 1.1b | Record size: `arc_owner` verdicts repeat ~1,800 glyph ids per arc in `considered` (57-75% of a record) and `__main__` writes `indent=2` (x1.7 on short rows). Dedupe `considered` to one system-level reference and write compact JSON; `positional_store.stream_observations` must stop depending on `indent=2`. Target < 20 MB/page on Breitkopf | **done** (`claude/sharp-cohen-5a1011`, on top of the compact-JSON half at `6b7dd3a3`). **Compact JSON** (first half, unchanged): `staged/__main__.py` writes `separators=(",", ":")` to `--out`; `positional_store.stream_observations` streams with `ijson`, format-agnostic (`test_positional_store.py::test_stream_observations_is_FORMAT_AGNOSTIC`). Measured alone: Litolff 146.4 → 78.3 MB (19.6 MB/page), Breitkopf 461.1 → 245.1 MB (61.3 MB/page, 3x over). **`arc_owner` dedupe (second half)**: measured per FIELD first (`benchmarks/omr-ink-gather-2026-09/FINDINGS.md` §12) — an `arc_owner` verdict is **99.2 % three copies of one id set** (`basis` 37 %, `correlated` 31 %, `considered` 31 %; `detail` 0.4 %), and `considered − used` is IDENTICAL for every arc in a system (exactly 1 distinct set per system, 7 systems, both documents): the harness faithfully recording `adjudicate_arc_owner`'s one system-scoped `Q.GLYPH_BOX` read per arc. The content cannot shrink (`basis` feeds `Log.closure` and the circularity filter), so the fix is SPELLING, in the file only: `tools/omr/staged/record_io.py` pools lists that repeat within (quantity, decider, system) as one core under `record.pools` plus each verdict's private ids at their positions (`{"$pool": id, "ins": [[0, "obs:…"]]}`), interns repeated `correlated` groups by content, and `load_record` expands a file back to exactly the dict `Log.to_json` produced. `Log`/`Verdict`/`Evidence`/every stage untouched; the in-memory `result` the CLI hands `--musicxml` is never pooled; a list under 64 ids or a group of one is written inline, so small fixtures are byte-identical. **Measured** (`probe/pool_control.py`, compact → pooled): Litolff **75.0 → 32.2 MB, 8.0 MB/page**; Breitkopf **237.3 → 52.7 MB, 13.2 MB/page** — both under budget; 22 % / 11 % of the original `indent=2` files. Pooling costs 0.9 s / 2.7 s at write. Controls that can fail: the writer expands its own output and raises `PoolMismatch` on any difference (tested by breaking `_decode`); `expand(pool(record)) == record` on every key of both real records while bare `json.loads` of the same text is REQUIRED to differ (89 / 100 pools); `trace --empty-claims` byte-identical on both documents through the routed loader; a dangling reference raises. **Blast radius enumerated, not feared**: only readers of the three id-list fields see a difference — `trace.py`, `brakes.py`, and six benchmark scripts (`reinfer.py`, infer `probe/reach.py`, plumbing `probe/edges.py`, `dynamics_arm.py`, meter-boundary `summarize.py`, duration-beams `readjudicate.py`) — all routed through `record_io.load_record`, as are every in-tree loader that hands a record on (`export`/`lilypond` CLIs, `inventory`, `wiring.with_run`, `acceptance`, `factsheet`); `record_slim` copies pools and refs through (tested); `byte_share.py` counts `record.pools`. ⚠️ **A record FILE is read through `record_io.load_record`, never bare `json.loads`** (DECISIONS 2026-09-22): a naive reader sees a dict where a list was and a `quantity_of.get(rid)` walk silently drops it. Registered `record_io.py` in `reach.NOT_A_STAGE` (the guard tripped on arrival, as designed). After pooling, no quantity exceeds a fifth of Breitkopf (`duration` 9.7 MB, 18.8 %). Not measured: whole-movement pages (1.1's gathers are the first data), and `Evidence.correlated_groups`' O(n²) pair walk per verdict (~1,800² × 2,207 on Breitkopf) — a wall-time cost, flagged in FINDINGS §12.6. `pytest -m "not slow"` and `staged.check` figures in the commit message.
| 1.2 | Run-time budget; `OMR_DIRECTION_TEXT_SCAN_GATE` on for acceptance; movement in < 1 h unattended | **done** (gate met 2026-09-23; budget line split out as 1.2b) (`claude/decide-stage-cost-1.2`). Tonight's first whole-movement run spent GATHER's estimated 25 min then ~12.3 h in ADJUDICATE+groups+EVALUATE+INFER on 16 pages -- profiled per decision on the 4-page Litolff shared record and found TWO O(n^2) mechanisms, both fixed in the ADJUDICATE harness (`record.py`/`adjudicate.py`), neither touching any adjudicator body or any verdict's value: (1) `Log._ids`'s SELF_AND_DESCENDANTS branch rescanned the WHOLE log (every quantity, ~45,000 rows) on every call -- `adjudicate_arc_owner` alone makes 779 such calls; now a per-quantity index + per-(quantity, subject) result cache, both invalidated by a write counter, so a write to quantity X is visible to the next read of X whenever it happens (stronger than "never crosses a decision boundary"). (2) `Evidence.correlated_groups()` did an O(n^2) all-pairs walk over everything a decision consulted (`_seen`, ~770 rows for every one of `arc_owner`'s 779 arcs) plus an unmemoised `Log.closure()` BFS per pair -- measured at 26x the rest of the decision (8.6s vs 0.34s / 64 arcs); now a bucket-by-(reader,frame,quantity) + union-find pass, `closure()` memoised (a pure function of an immutable `basis`, so nothing to invalidate). **Combined, unprofiled: ADJUDICATE 29.9s / GROUPS 1.9s / EVALUATE 13.4s / INFER 0.3s on the 4-page record** (was: did not finish `arc_owner` within ~100s of a killed profiling run; a base-vs-arm control over the same 13-decision/one-system subset ran the UNPATCHED code to 468 CPU-seconds and still had not finished, against the fix's 16.461s -- a measured **28x floor**). Correctness: two randomised/property test suites (`test_staged_record.py::TestDescendantsCache`, `test_staged_adjudicate.py::TestCorrelatedGroupsRewrite`, 25-trial), each checked against a reimplementation of the pre-fix algorithm -- NOT a byte-identical full-pipeline control, because the baseline run did not finish in time (see FINDINGS.md's own control section). `pytest -m "not slow"`: 2822 passed (was 2818), 3 skipped, 0 failed; `staged.check`: 255 open, all `status=ok`. ⚠️ **What is NOT done**: the 16-page whole-movement re-run this fix should now make tractable was not taken (time-boxed session ended first) -- `gather_movement.sh`'s own budget-estimate text (its step 5) is UNCHANGED and still covers GATHER only; a residual second-order cost is named but not fixed (`Q.DURATION` is read AND revised at CELL granularity in EVALUATE, so its per-quantity index rebuilds roughly once per cell rather than once per rule -- bounded, ~10s on this record, but not free); `OMR_DIRECTION_TEXT_SCAN_GATE` was already ON in `gather_movement.sh` and untouched. Also landed: the coordinator's clean-tree-guard addendum -- untracked files under `benchmarks/acceptance/out/` and `library/` no longer trip the guard, tested against a real status-porcelain fixture (`test_clean_tree_guard.sh`). See `benchmarks/omr-decide-stage-cost-2026-09/FINDINGS.md` for the full per-decision table, the control, and what is not established. **LANDED 2026-09-23** after the gate was completed by the landing session (FINDINGS.md §3b-§5, written by that session and marked as such): (a) the identical-verdict control now RUNS TO COMPLETION on both sides — the lane's control adjudicated all 7 systems and only filtered to one when dumping, which is why base could not finish; restricting the WORK to one system gives base **16.184s** against the arm's **3.480s** (4.65x) over the same 429 verdicts, and **0 of 429 differ** on subject/quantity/outcome/value/reason/basis/considered — the brief's bar, MET. `correlated` differs on 52, and all 52 are exactly the documented removal of one self-paired SINGLETON group (0 unexplained), so the lane's asserted behaviour change is now confirmed on real data rather than only claimed. (b) ⚠️ **One of the rewrite's stated proofs was FALSE and was repaired before landing**: the bucket-representative shortcut assumed `_one_signal` does not depend on the specific observation, but it does through `closure(b)`, and `Observation`'s own docstring records that the empty-basis invariant was found too strong on 2026-09-07 — two rows can share a (reader, frame, quantity) bucket with disjoint closures, so the grouping would have turned on INSERTION ORDER (`rep_bucket_probe.py`). Repaired with a strictly faster rule: an empty-basis observation provably cannot be one signal with a non-observation, so that population is skipped outright, and the rare derived ones are tested member by member. Latent, not live (today's one derived producer uses a single source row), and covered by a test run RED first. |
| 1.2b | **The run script's budget estimate still covers GATHER only.** `gather_movement.sh`'s step 5 prints `n x 93s` (and the no-gate upper bound) and says nothing about ADJUDICATE / GROUPS / EVALUATE / INFER / write — the stages that turned a 25-minute reading into a 12.8-hour run on 16 Beethoven pages. The estimate must cover all of them, using the 16-page Litolff run (GATHER ~25 min; ~12.3 h for the rest, single core, 17 GB) as its data point, and must say plainly that the post-GATHER figure predates 1.2's two fixes and is therefore an UPPER bound until a 16-page re-run replaces it. This was the second of the two defects 1.2's brief named; 1.2 fixed the clean-tree guard (with `test_clean_tree_guard.sh`) and left this one untouched, which its own FINDINGS.md states. | todo |
| 1.3 | `python3 -m tools.omr.acceptance` → `benchmarks/acceptance/current.json`, side-by-sides, nightly | **done** (`claude/acceptance-1.3`) — `tools/omr/acceptance.py`, `benchmarks/acceptance/manifest.json`, `tools/omr/tests/test_acceptance.py` (28 tests). Runs today's inputs: the two four-page shared scan records + the committed engraved 24-bar fixture (whole-movement records are 1.1, still todo — the manifest and each doc's `caveats` say so explicitly, never silently). Whole run: **9.3s wall**, well under the 15-min per-step cap. Measured: notes-reaching-file Litolff 965/2347 (0.411), Breitkopf 2262/3337 (0.678), engraved 358/371 (0.965); bars-add-up-to-meter-in-force (per-bar, from each measure's own `<time>`, never a constant) Litolff 1116/1332, Breitkopf 258/794, engraved 412/450; parts named 12/12, 14/14, 18/18; held_out (`staff_not_identified`) 783, 238, 0 — **both scan figures are STALE**, from records gathered 09-17/09-22 before the 2.1b INFER merge (`ec7e42f4`, same-day but later) landed, flagged per-document; engraved reading pooled F1 **0.947** (frame-shift control collapses to 0.058 — position, not luck), OMR-NED **0.1519**, LilyPond compiles, PDF produced, 3 barcheck failures. Side-by-sides (print crop + best-effort Verovio render of the count page's own bars) built for all three. Found and fixed one live bug in the process: `benchmarks/omr-staged-engraved-2026-09/staged_reading.py`'s synthetic page never set `page_index`, which `score_reading._page_of`'s same-day rewrite (`dcdff9f2`, 13 min later) now requires — one-line fix, noted at its site. |
| 1.3b | The acceptance command exports a shared record's EXISTING verdicts and never re-adjudicates, so a new decision (2.4a) leaves `current.json` unmoved until the records are regathered — tonight's 1.1 records are fresh and resolve this; if 1.1 slips, add a re-adjudicate step | todo (resolves with 1.1) |
| 1.4 | First cleanup count on the three fixed pages (DECISIONS 2026-09-22) — Sean | **Beethoven page counted 2026-09-23, BY THE LANDING SESSION AND NOT BY SEAN** (`benchmarks/omr-cleanup-count-2026-09/counts/beethoven5-litolff-p3-2026-09-23.csv`, 26 rows, **total 34 fix-actions**: missing 8, wrong 22, spurious 2, would-not-notice 2). ⚠️ CATEGORIES §2c defines `would-not-notice` as **the editor's** eye — Sean's — so this is a PROXY for the instrument, not the instrument, and it is the weakest form of the evidence because the counter is the system that produced the output; `counts/README.md` says so at the top. **Sean's own count on this page is still todo**, as are Brahms and the engraved page. What it found, in order of size: (1) **not one printed accidental reaches the file, anywhere in the movement** — 1,531 glyphs detected, **0 read into a verdict**, 0 `<accidental>` elements across 7,878 sounding notes, every `<alter>` coming from the key signature alone; the 18 counted are a declared SAMPLE (4 string staves, bars 49–56, ±3) and the rest of the page is uncounted, not zero. (2) **key signatures are re-read per system and wander** — 4 of 12 parts wrong at bar 49 (Violin II reads **+1** on a 3-flat page; Viola jumps to **+7** at m64), while Clarinet −1 and the horn/trumpet 0 are correct, so the reader is not merely defaulting. Recorded as going RIGHT: the suppressed 8-staff system is handled correctly (Oboe/Trumpet/Timpani tacet → measure rests), all 12 parts carry real instrument names, meter is 2/4 and right, and the 1,971 `arpeggiato` misdetections cost the human nothing because none reaches the file. The `missing` column was re-measured against the REFERENCE ENCODING through the hand-verified `984073-p3` window row rather than eyeballed off the 725-px crop, and moved 5 → 8 (Corni s1, Fagotti s2, Viola s2 all short by 3+ where the first pass had called them within reading error); the join carries a control that can fail and passes — the three staves the print suppresses in system 2 sound in 0 reference bars and ours writes measure rests for exactly those. Scope vocabulary hit its gap a second time (a key wrong across a part's whole page-range is one gesture that is neither `staff-system` nor `page`) — flagged for the next counter, not silently absorbed. **SET UP FOR SEAN 2026-09-23 (`fd656c20`)**: three self-contained per-system print-beside-ours pages at the gather's own DPI (`benchmarks/omr-cleanup-count-2026-09/out/count-<doc>-<page>-2026-09-23.html`, system crops 2,393 px wide against the 725 px he rejected), three EMPTY sheets `counts/<doc>-<page>-SEAN-2026-09-23.csv` (only page/system/part/bars pre-filled), `counts/HOW-TO-COUNT.md`, and `part-range` added to the scope vocabulary (CATEGORIES §4c). **Sean's count on all three pages: todo — his to do, nothing to build.** |

Gate: `current.json` for all three; a baseline count committed.

## Phase 2 — Close the foundation, in funnel order

| item | what | status |
|---|---|---|
| 2.1 | Identity is PLACEMENT, never naming; gate `staff_not_identified` 783 → < 100, zero grafts against print | 783 → 141 by `OMR_SLOT_FAMILY_BLOCK` (09-21); constraint channels (`claude/instrument-identification-channels-8dcaa2`: 2acb3fcf, a0a5aeac, 73d7ffbe, f934ae7c) move 13 placements INFER → ADJUDICATE, count unchanged. Residual = five condensed `Violoncello e Basso` staves. **Decided 2026-09-22** (DECISIONS; `benchmarks/omr-cello-bass-convention-2026-09/`) → 2.1b. Identity branch merged to main at 4cc82433 (adds 55d50393, a test narrowed to its meaning, mutation-tested). **Gate met by 2.1b: `staff_not_identified` 141 → 0, zero grafts** — done |
| 2.1b | Place a [Cello, Contrabass]-narrowed staff on BOTH slots: line to Cello, doubled with `<transpose>` −8ve and `condensed_from` to Contrabass; division text as `<words>`; gate 141 → 0 on Litolff with the 5 staves named, Breitkopf unchanged (it never condenses) | **done** (`claude/condensed-cello-bass-2.1b` @ ec7e42f4, merged with main 4cc82433, fast-forwarded to main). `inferences.collapse_slot_index_to_family_block` collapses the last block member onto the Cello slot ONLY where its own two candidates resolve to [Cello, Contrabass] (via the reference's own `Q.INSTRUMENT`), detail `condensed_with_slot`; `staged/export.py`'s slot join doubles via `_condensed_double` (directions/arcs/glyph/marks stripped so post-join wedge/arc passes can't find it twice) and `_insert_transpose` (needs `<diatonic>0</diatonic><chromatic>0</chromatic><octave-change>-1</octave-change>` — music21's own reader needs `<diatonic>`, not just the schema-required `<chromatic>`); new `notes_doubled_to_condensed_slot` balance bucket, equality held. Measured on the merged tree (`origin/main` 4cc82433 + this branch) over the two shared records: Litolff — exactly the 5 named staves collapse, `staff_not_identified` 141→0, parts stay 12, `notes_doubled_to_condensed_slot`==141, balance holds, all 5 confirmed against the print crops as the bottom string staff of their system (p4/s0's separate Vcl./Basso correctly untouched), full file round-trips through music21 (Contrabass 107 notes); Breitkopf — 0 family_block inferences at all, so 0 impact, confirmed byte-identical by construction. Division text: `direction_lexicon` carries none of "senza Bassi"/"col Basso"/"Vc. soli" — recorded as a finding, not widened (out of scope). `pytest tools/omr/tests -m "not slow"`: 2714 passed. `staged.check`: 252 open findings, identical to `origin/main` alone (this branch adds zero). |
| 2.1c | `instruments.lookup` resolves every dual label (`Violoncello e Basso`, `Violoncell u. Contrabass`, …) to Cello alone, discarding the second instrument on 26 held editions; a bare `Basso.` is a singer without the roster | **done** (`claude/dual-label-lexicon-2.1c`). `Match` gains an additive `condensed_with: str \| None` — the second instrument where a label joins two instrument nouns with a conjunction (`e/ed/u/und/and/et/&`) and BOTH halves resolve through the EXISTING lexicon (`instruments._condensed_partner`; an ambiguous other half — `basso` is Bass voice or Contrabass — resolves toward whichever declared alternative shares the PRIMARY's family, so `Violoncello e Basso` names Contrabass, not a singer). `lookup`'s own answer is UNCHANGED: replayed against all 1,422 real margin labels (`benchmarks/omr-lexicon-2026-09/resolve_labels.py`), 0 of 1,422 primary resolutions moved. Reach: 2 of 1,422 reader-emitted labels gain a partner (`Posaune u. Tuba` ×2 — a real condensed brass pair, not cello/bass); over the catalog's edition rosters, 36 hold a candidate dual Vc/Basso label and 29 now name the pair, the 7 holdouts being OCR-truncated (conjunction at the string's edge, one side empty) or bare juxtaposition with no conjunction at all (`Vc. Cb.` — deliberately not treated as evidence: no new alias, no guess). Bare `Basso.`/`Bassi` untouched (23 of 1,422 unmoved); the roster/score-order channel already rescues 3 `Basso.` occurrences to Contrabass where the work is catalogued — measured (`benchmarks/omr-producer-consumer-2026-09/roster-reach.txt`), not touched. Wired into STAGED `adjudicate_instrument`'s verdict `detail` only (`staged/adjudicators/identity.py`), never its value; NOT wired into `collapse_slot_index_to_family_block` / `_condensed_double` — measured reach is ZERO `Q.MARGIN_LABEL` dual labels on both shared records (Litolff prints no continuation-system string labels; Breitkopf never condenses), so the label-derived case is left as the next consumer, unbuilt. `staged.check` TOTAL unchanged at 252/252 (the new detail key is out of `wiring`'s DETAIL scope, which only walks GATHER-stage rows). `pytest tools/omr/tests -m "not slow"`: 2714 passed, 3 skipped, 0 failed. LEGACY path untouched in behaviour — same `Match` object, additive field only. |
| 2.2 | Key-signature precedence, engraved-only one-sided; gate 20/20 engraved, Litolff ≥ 44/75 | **landed** `claude/identity-conditions-key-signature-b510b3` (`beba5c64` is the default flip; `291677b5` the rule). Engraved gate **exceeded**: 47 of 50 right, 3 wrong, and the template is 67/67 wherever it speaks. ⚠️ **The Litolff half of the gate is NOT measured** — the rule is one-sided and proved a no-op on a real scan gather, so it cannot have moved 44/75, but *cannot have moved it* is not *measured at ≥ 44/75*. ⚠️ n = 1 engraved document, 1 renderer, no print consulted; a second engraved document is the ranked next step and the 10 held candidates are named in `benchmarks/omr-document-identity-2026-09/out/library-domains.json` |
| 2.3 | INFER duration rules against the print (17 + 41 crops) → default decision | **evidence cut, default NOT decided** (`benchmarks/omr-infer-duration-print-2026-09/`). `reinfer.py` control first: 19,563/19,563 verdicts reproduced, 0 differ. With `OMR_INFER=1` on the Litolff shared record the two rules make **16 inferences** (6 by column, 10 to barline). **2 of 16 override the reader's own candidate ordering** (pick support 1.0 over 2.0) — `forbids_argmax` doing real work, and the cases worth reading first. `probe/crop_inferred.py` cuts a 600-dpi crop per inference with corner brackets on the exact head; **the frame control was run in a state where it FAILS** (wrong dpi → contrast negative, 16 of 16 refused) before being trusted where it passes: **10 written, 6 refused**, and the margin was NOT lowered to recover the two marginal pairs. ⚠️ **Three things block the default decision, all named in FINDINGS**: (a) the record carries **0 `notehead_is_not_a_notehead` verdicts** — it predates 2.4a, so the population is one today's tree would not hand the rules unchanged, and the reach figure is an UPPER BOUND; the sharpest case is one of the two ordering-overrides, and it is now CLOSED: `glyph/2/0/9/15/5` has canonical height 47 (**0.470 spaces**, under `CLIPPED_NOTEHEAD_MAX_SPACES` 0.6) and its `bbox_page_px` top edge is **0.00 px** from its cell's top against a 1.0 px tolerance, so **2.4a's `clipped_fragment` FIRES and today's tree refuses this glyph as not a notehead** (`probe/would_2_4a_refuse.py`). ⚠️ **Neither ordering-override survives**: this one is not a notehead, and the other (`glyph/4/0/9/5/7`) fails the reference membership check by moving away from the encoded duration — so the two cases that were supposed to show the stage buying something a consequence could not are one non-notehead and one wrong answer. Swept over all sixteen, **2.4a would refuse 1 of 16 (6%)** — so the 'reach is an upper bound' caveat is correct but SMALL, and the pre-2.4a record does not inflate reach materially. Two shapes nothing refuses: `glyph/4/0/9/5/7` is flush against the cell edge (0.00 px) at 0.79 spaces, just over `clipped_fragment`'s 0.6 ceiling and the one that fails the reference check (suggestive the net is tight, NOT a reason to retune a threshold measured against 255 print-adjudicated boxes), and `glyph/4/0/8/2/3` is 3.12 spaces tall, a merged blob no refusal targets. ⚠️⚠️ **ADJUDICATED BY SEAN 2026-09-23, AND THE FINDING MOVED OFF DURATIONS.** He found the CROP defective first — *"there is a staff at the top and a staff at the bottom - i dont know which staff the cell is focussing on"* — and he was right: a window centred on a note in the GAP between two staves commits to neither, so it cannot tell a correct attribution from a cross-staff error. The probe now names the filed staff and traces its own five `Q.STAFF_LINES` in green; all 10 crops re-cut. His reading and the recorded geometry agree on both subjects, and **BOTH ARE MISATTRIBUTED**: `glyph/4/0/9/5/7` is filed on staff 9 Violoncello but sits one space above staff 10 **Basso**'s top line, and `glyph/2/0/9/15/5` is filed on staff 9 Viola but sits 1.3 spaces below staff 8 **Violino II**'s bottom line — each a first-ledger-line note in the gap, given to the neighbour. **This REVERSES the §5b verdict**: that row was scored against Violoncello m88 `[1.0,1.0]`, the wrong part; against **Basso** m88 the reference is `[0.5,0.5,0.5]` and Sean reads an eighth off the print, so the **READER'S TOP (0.5) IS CORRECT and the rule's 0.25 IS WRONG**, print and encoding agreeing against the rule. ⚠️⚠️ **CORRECTED, FINDINGS §8 — `glyph_owner` IS NOT THE CULPRIT AND THE DEFECT IS NAMED.** Chasing Sean's own convention (*"notes should never be that far away from a staff unless there are ledger lines close to the staff connecting the note conceptually to the staff"*) overturned this lane's first diagnosis, twice. (a) `glyph/4/0/9/5/7`'s ownership verdict reads `decided value "staff/4/0/10" reason "distance"` — **staff 10 IS Basso, exactly where Sean puts it**; ownership got it RIGHT without needing the ladder, and the claim that both subjects are `glyph_owner` failures was wrong. The other subject has NO ownership verdict at all — it never entered the contest; two causes, not one. (b) §7b's "197 contested and resolved to the FARTHER staff" was **unfounded** — that sweep checked only whether a verdict EXISTED and never read its VALUE. Reading the values: **189 resolved to the NEAR staff (correct), 161 never contested, 8 resolved elsewhere** — `glyph_owner` is largely working, and offering presence as direction was the error CLAUDE.md names. **THE ACTUAL DEFECT**: both duration rules declare `reads=(Q.ONSET_COLUMN, Q.EVENT, Q.DURATION, Q.GLYPH_BOX)` with **`Q.GLYPH_OWNER` ABSENT** (`inferences.py:298`, `:389`), so they walk glyphs by their DETECTION CELL and use that staff's columns and neighbours as witnesses for a glyph ownership has already awarded elsewhere — on `glyph/4/0/9/5/7` the record says Basso and the rule inferred 0.25 from **Violoncello's** column structure, where the note is a Basso eighth. CLAUDE.md's standing bug class: **the value existed and nothing read it**, and since the declaration is the enforcement it is a one-line omission with a measured consequence. Sean's convention is confirmed by the geometry (1.41 / 1.55 spaces from the staff he names — first-ledger-line distance — against 3.60 / 3.77 from the filed staff), and the rungs that would prove it were NOT DETECTED (nearest x-overlapping `ledgerLine` 7.34 spaces away; the other cell holds zero), which is the same recall gap that made 2.4a's `unladdered` net negative. 2.3's question is now **"are these rules reading the right glyphs at all"**, not "are they right". The 161 never-contested glyphs remain a separate question about `glyph_owner`'s DOMAIN (`subjects_from`). **FIXED AND RE-MEASURED 2026-09-23 on Sean's instruction** (*"add Q.GLYPH_OWNER to the rules' reads and re-measure"*), after he adjudicated a third crop the same way — 3 of 3 where his reading, the ownership verdict and the geometry agree. Both rules now declare `Q.GLYPH_OWNER` and `_events_with_x` DROPS a glyph whose ownership names another staff (dropped, never relocated — CLAUDE.md §10; and **silence is not a verdict**, an uncontested glyph is KEPT, which is 13 of the 16). Control unchanged at 19,563/19,563. Reach **16 → 13**: `by_column` 6 → 6, `to_barline` 10 → 7, and **0 surviving inferences changed value** — the fix is purely subtractive, because the rules had no mechanism to reach the right answer for ink they should not have read. ⚠️ **Three dropped where two were predicted**: the third (`glyph/2/1/0/13/5`) has NO ownership verdict and was kept by the guard, but sits in the SAME BAR as a dropped foreign note — removing it changes that bar's event structure, so *is there a further onset in this bar* is no longer the same question and the rule declines. A knock-on that produces a REFUSAL is the safe direction. §9b checked whether EXPORT has the same blind spot: **it does not** — `export.py:971` already drops a relocated copy into the named `owned_by_another_staff` bucket, so the omission was confined to these two INFER rules and the fix applies the exporter's own discipline. Gate: `pytest -m "not slow"` 2,823 passed; `staged.check` 255, identical to main; `TestItReadsGlyphOwner` run RED first with two positive controls so it cannot pass by refusing everything. ⚠️ **Both rules remain default OFF** — removing three wrong inferences does not show the other thirteen are right, and 2.3's default decision is still open. Branch `claude/infer-reads-glyph-owner`. Still n=1 document, 14 inferences unexamined; (b) no crop has been adjudicated by anyone — `VERDICT_none_yet` is null on every manifest row, for Sean; (c) Breitkopf not run, and the earlier findings record the funnel INVERTING between the plates. The reference-encoding score WAS then taken on the two pages whose join is clean (11 of 16; page 2 excluded because the verified `984073-p2` row records this raster dropping one barline at m19|m20, 16 against the print's 17). ⚠️ **Its first draft reported '6 of 6 inferred correct' and that number was worthless** — it compared left-to-right noteheads against a PART-MAJOR note list on two-part staves, and counted HEADS where the reference counts NOTES (`cell/4/0/0/12` is 4 heads at 2 distinct x = 2 events). Repaired to group heads into events and align per part, the honest result is **0 of 11 rows INFORMATIVE**: every positionally comparable bar has a rhythmically UNIFORM reference (all 1.0 or all 2.0), so every alignment agrees and no index could be wrong — a check that cannot fail, reported and excluded rather than counted as passes. That is a result about the INSTRUMENT: the rules fire where neighbouring staves agree, which is where rhythm is even, which is exactly where the encoding cannot discriminate. The weaker membership check CAN fail and does once: `glyph/4/0/9/5/7` (m88 Violoncello, the higher-confidence ordering-override at 0.77) infers **0.25 where the reference bar holds only 1.0**, moving FURTHER from the reference than the reader's own 0.5 — though that bar is over-read before the rule runs (our 4 events against the reference's 2 notes), so it shows the rule does not repair a bad bar rather than refuting the rule. The other 10 rows have inferred == reader's top, so they test the reader, not the rule. Also surfaced by the frame control and NOT chased: `staff/4/0/1`'s recorded `Q.STAFF_LINES` land on WHITE (contrast −12.25), a staff-detection fault. **DEFAULT DECIDED ON 2026-09-23 (Sean) — `6f269360`.** `OMR_INFER` flipped to default ON with a deny-list off test; replay control 101,361/101,361 (Litolff) and 238,473/238,473 (Brahms) reproduced, 0 differ. OFF→ON on one tree: Litolff 0→69 inferences (20 by column, 49 to barline), `<note>` 12,375→12,424, `duration_narrowed` 1,376→1,321; Brahms 0→287 (174/113), 22,922→23,145, 3,583→3,339; census balanced with `unaccounted` empty on all four arms; INFER wall 1.4→10.6 s and 3.4→20.8 s. ⚠️ The funnel INVERTS between plates (Litolff mostly to-barline, Breitkopf mostly by-column) and **Breitkopf has 0 subjects checked against its own print** — the three print-checked subjects are all Litolff. `benchmarks/omr-infer-default-2026-09/FINDINGS.md` §7. A session that finds a rule wrong on a crop files evidence and asks Sean; it does not flip the default back. |
| 2.3a | Engraved fixture bars 21–22 read ONE QUARTER SHORT of 2/4 on 17 of 18 parts (found by the LilyPond bar-check, confirmed by summing `<duration>` in the MusicXML) — a duration fault on perfect ink, in the record before any exporter; diagnose in the record with `trace --subject` | todo |
| 2.4a | Notehead precision on staged: port the two legacy filters; width floor as an ADJUDICATE refusal | **done** — `adjudicate_notehead_is_not_a_notehead` (`Q.NOTEHEAD_IS_NOT_A_NOTEHEAD`, ordered before `Q.GLYPH_OWNER`; `tools/omr/staged/adjudicators/notehead_precision.py`). Two rules SHIP: `clipped_fragment` (ports `transcribe._drop_clipped_notehead_fragments`) and `too_narrow` (the measured, never-shipped 1.0-staff-space floor). A third, `unladdered` (ports `transcribe._drop_unladdered_noteheads`), is MEASURED NET NEGATIVE against the print on both acceptance scans (costs 4 of 29 / 4 of 74 confirmed real noteheads against 0/2 confirmed non-noteheads caught — a same-cell ledger search cannot see rungs the detector missed in-cell) and is held back (`UNLADDERED_SHIPS = False`; its signal is still computed and recorded per notehead). The shipped pair costs 1 of 29 / 1 of 74 confirmed real noteheads and catches 10/19, 32/44 confirmed non-noteheads on the crop-pass print join; base-vs-arm on the two shared records (isolated re-adjudication, not a full 28-decision rebuild): Litolff notes written 965→899, Breitkopf 2262→1837, balance equality holds exactly in both arms. `no_stem` overlap: 110/793 (14%) Litolff, 539/1233 (44%) Breitkopf. ⚠️ `python3 -m tools.omr.acceptance` shows NO delta on the merged tree — it exports the FROZEN shared records without re-adjudicating, and those records predate this decision; a re-gather/re-adjudicate is needed before `current.json` reflects this. 23 tests, run RED first against the pre-branch tree (22/22 failed on the missing quantity). `pytest tools/omr/tests -m "not slow"`: 2,782 passed. `staged.check`: 251 open (unchanged from origin/main alone). 12 print crops (uncropped/unadjudicated by this job) at `benchmarks/omr-notehead-precision-2026-09/out/print/`. Not established: `glyph_owner`'s contest scoring does not consult this decision (a refused glyph can still make a genuine partner lose a contest — needs a GATHER change to close); n = 2 documents, both scans; `clipped_fragment`'s 1-of-1 catch/cost pair on this join is too small to be a result on its own, shipped on its legacy heritage. sha 7f0e8850, merged to c603ca42. — **print-checked by Sean 2026-09-22: 12 of 12 refusals correct**, slivers are the neighbour staff's notes, boxed there |
| 2.4b | Detector recall on hollow noteheads and hairpins: one labeling round + head surgery | todo |
| 2.4c | Unread-mark subject: zero-coverage mark-sized ink at a corroborated column → marked bar, never a note | todo |
| 2.5 | Arcs — re-measure after 2.4 | blocked (2.4) |
| 2.6 | `glyph_owner`'s DOMAIN: the contest's identity test reads the disputed quantity | **BUILT, MEASURED AND MERGED 2026-09-23, not print-adjudicated** (`claude/owner-domain-2.6` `99f9e185`; `benchmarks/omr-owner-domain-2026-09/FINDINGS.md`). GATHER only: `category` replaces `smufl_name`, `CONTEST_IOU` back to the legacy swept 0.3. Never-contested 161→74 / 704→330 / 1392→733 (87, 374, 659 handed in — §10 predicted 87 and 374 exactly); pre-existing `glyph_owner` verdicts changed **0** on all three records; base arm reproduces each record 1386/1386, 6013/6013, 24795/24795 (RED arm 1365/1386); balanced, `unaccounted` empty; a real base-vs-arm gather on Litolff p3 agrees at +107 of the same 107 glyphs on identical detections. Handed-in outcomes 85 near / 2 tied / 0 own (p1–p4). `<note>` 12,424 → 12,258 (Litolff), 23,145 → 22,886 (Brahms): the loser copies now dropped. ⚠️ `category` is COARSE: dynamics fall 205→176 / 704→622 / 1307→1167 (77 of 86 newly-contested p1–p4 dynamics are the same letter twice) and Breitkopf's `structural` is 3285 of 6259 newly contested — the legacy reader's own behaviour, unadjudicated. **PRINT-ADJUDICATED BY SEAN 2026-09-23: 18 of 18 noteheads on the right staff (moved 6/6, stayed 6/6, controls 6/6; `out/print/ADJUDICATION-sean-2026-09-23.json`).** The 19th crop (`glyph/4/1/3/4/3`) is **not a notehead**: a `noteheadBlackOnLine` box at 0.61 around a printed *f* (forte), no dynamic box on that ink, `stem_direction` abstained `no_stem`, and it is in the file as a G2 quarter — a detector-precision miss outside 2.6's question, handed to 2.4 (a black head that is a quarter by duration and has no stem is a contradiction the record already holds and nothing reads). Design record:  (`64c83c5f`; `benchmarks/omr-infer-duration-print-2026-09/FINDINGS.md` §10, `probe/owner_contest_domain.py`). All 161 never-contested near-neighbour noteheads (p1–p4 record; 704 of 1,725 on the whole movement, same shape) lack a `Q.GLYPH_BAND_DISTANCE` row of ANY state — `adjudicate.subjects_for` never made them subjects; 0 are abstentions, 0 are scope faults. Cause, measured to the glyph: `gather_ownership_evidence` (`gather.py:647`) restates the frozen legacy contest (`_dedupe_cross_staff_detections`: `category` equality, IoU > 0.3) as smufl-NAME equality and `CONTEST_IOU` 0.5 — and `…OnLine`/`…InSpace` encodes position relative to a staff, the very thing in dispute. Name rejects 83 (38 the same head spelled OnLine vs InSpace), IoU rejects 69 (50 above the legacy 0.3), 9 have no twin on the near staff at all (the pad reaches; the detector did not fire — NOT a pad problem). The 8 'elsewhere' all resolved to the glyph's OWN staff (5 `range_veto`, 2 `ladder`) or abstained `tied` (3); 0 to a third staff. The `tied` mechanism is a finding of its own: a vetoed candidate scores exactly −6.0 through the correlated-group collapse, so two vetoed candidates 2.8 spaces apart tie. **The process**: a GATHER change only — compare `d.category` and return `CONTEST_IOU` to the measured 0.3; `subjects_from=Q.GLYPH_BAND_DISTANCE` is correct and must not move (a glyph awarded elsewhere has a twin by construction, which is what makes the drop safe). Hands in 87 of 161 (374 of 704); the 38 with no twin must NOT be handed in (a drop turns a wrong-staff note into a missing one). Never relocate, never widen the pad, never IoU 0.25 (drops three correct notes on Brahms), never convert `tied` into an answer. Gate: reach printed first on BOTH scans; the 189 correctly-contested stay 189 with every changed verdict named by subject; `unaccounted` empty; then crops at the gather's DPI with the filed staff's lines drawn and a positive control from the 189. Needs two full re-gathers, shared with `omr-staged-dedupe` §6.1/§6.6. ⚠️ Not one of the 161 has been read against a print; §10 says why they were never ASKED, not what the answer is. |
| 2.7 | Read the printed in-bar accidental; a DECIDED reading overrides the key for that pitch to the bar's end (Sean, 2026-09-23) | **PARKED 2026-09-23 (Sean): funnel code committed UNMEASURED on `claude/accidental-2.7` (`69d64d95`), build stopped** — accidentals on wrong heads under wrong keys are wasted; resume after 2.8/2.9 and the notehead recall. Design: — Closes the largest `FAMILY_Q_IS_ELSEWHERE` gap: 1,531 accidental glyphs detected on the Litolff whole movement, 0 read into any verdict, 0 `<accidental>` across 7,878 notes — every `<alter>` comes from the key alone. **GATHER** `Q.ACCIDENTAL_STAFF_POSITION` (CLAIM.MEASUREMENT, mirrors `Q.NOTEHEAD_STAFF_POSITION`) on the GLYPH under its CELL, alteration kind off the class name. **ADJUDICATE** a new `accidental_owner` (`Q.ACCIDENTAL_OWNER`): the notehead immediately RIGHT of the glyph at the SAME staff position (C21/L32); abstains `ambiguous_height`/`no_candidate`. **EVALUATE** a downhill sibling of `respell_accidental` (cause `Q.ACCIDENTAL_OWNER`, effect `Q.ACCIDENTAL`, after `KEY_SIGNATURE`) supersedes the key-derived write on the owned note and every later same-(letter,octave) note in the bar carrying none of its own (C21); a natural cancels. **EXPORT** passes the value into the already-reused `_mxl_note`/`_MXL_ACCIDENTAL` legacy renderer; extends `accidental_reading`'s census with `applied`/`abstained_ambiguous`/`no_candidate` — no new refusal, `Unbalanced` untouched. **Claims** C21, L32, C26; **adds** one: a natural's cancellation is bar-scoped like any accidental (CLAUDE.md §10, unregistered). **ASK SEAN FIRST**: does Litolff print courtesy/cautionary accidentals in parentheses, and should those be `<accidental parentheses="yes">` or suppressed (C26: "no figure anywhere in the tree")? **Gate**: `<accidental>` emitted ≥ 18 on Litolff pdf-index-3 (bars 49–82), 0 wrong against a print check of the declared sample (4 string staves, bars 49–56) — reach printed first. **GATHER change**: needs two full re-gathers to price (§4d); `readjudicate`/`reexport_arm` are blind to it. Reuses `crop_inferred.py` (adapt for glyph+head), `status_census`, `trace --family accidental`. Prior art: LEGACY already pairs each glyph to the head immediately right at matching height (`transcribe.py:932-976`) and tracks the per-bar override with a natural clearing it (`transcribe.py:2206-2267`) — reuse the geometry, it is NOT print-verified anywhere. `e8cf5b26` fixed the opposite bug (key-derived `Q.ACCIDENTAL` wrongly emitted as a printed one). The 18 accidentals in the 2026-09-23 count's declared sample are the first print check. |
| 2.8 | EXPORT holds out a bar whose durations do not sum to the meter in force (Sean, 2026-09-23: *hold out — I want to know what we are getting correct*) | **DONE 2026-09-23** (`claude/bar-sum-holdout-2.8` `df46421d`, merged). `export._bar_holds_out`: per VOICE, chord = one event, tuplet scaled, a lone measure rest IS the bar, no meter → no verdict, pickups not special-cased. **0 exported bars whose sum ≠ meter on all three documents** by an independent parser importing nothing from the exporter (base 38 / 1,879 / 4,587 wrong). Count page: **68 of 408 → 0**, all 68 held, nothing else moved. Held out: engraved 38/449, Litolff 1,879/4,463 (42%), **Brahms 4,560/5,803 (78.6%, 3,105 of them judged against a CARRIED meter no reader confirmed — `Q.METER`'s reach is now that document's binding constraint)**. `<note>`: Litolff 12,375 → 8,588, Brahms 22,922 → 7,814 — the cost Sean accepted. Design finding: the meter judged against must be the FILE's (written only on change), not the record's; `_part_xml` now tracks `in_force`. `bars_add_up` retired to `bars_add_up_control`; `bars_held_out_sum` reports in its place. Full suite 5,051; `check` 255. ⚠️ Follow-ups named, not done: `size_measure_rest` leaves 42 Brahms measure rests at 4.0 in a 3.0 bar; `_divisions` not a multiple of 3 for ratio-only tuplets. OMR-NED worsened 0.1519 → 0.1629 on engraved, as §6b predicts for a note-deleting rule — not quotable either way. Original: a bar we could not read to the meter is a bar we could not read; it exports as the same marked empty bar as a bar we read nothing in, its heads counted under a NEW named refusal (`bar_does_not_add_up`), and the equality control still balances. Never pad, never trim, never re-time a bar to fit. Measured 2026-09-23 on the Litolff count page: **68 of 408 staff-bars** do not sum to 2/4 (Violin 20, Contrabass 9, Cello 8, Flute 7) and the exporter has no check at all. Gate: 0 exported bars whose sum ≠ meter on all three acceptance documents; the census names every held bar by (page, system, cell, staff); the machine proxy *bars that add up* becomes 100% BY CONSTRUCTION and is retired as a proxy — report `bars_held_out_sum` in its place. A chord's members are ONE event (§10 double-counting trap). A bar with a NARROWED duration is already held by `duration_narrowed`; this refusal fires only on a bar every event of which is DECIDED. |
| 2.9 | Key signature READ OFF THE DETECTOR'S HEADER BOXES; fitters corroborate; system agreement is a CHECK (Sean, 2026-09-23, amended after the trace — DECISIONS) | **LANDED AND MERGED 2026-09-23 WITH A NAMED REGRESSION** (`claude/key-majority-2.9` `f7b18786`; `benchmarks/omr-key-majority-2026-09/FINDINGS.md`). The majority was priced and REFUSED (8–8 on the acceptance page). Shipped: `Q.KEYSIG_MARKER` as the primary reader, read as a ladder of SLOTS not a count of boxes (a neighbour's flat bleeds into the pad; unit `Q.CELL_STAFF_SPACE`), abstaining `mixed_marker_kinds`/`natural_markers`/`too_many_markers`; fitters corroborate, a disagreeing fit recorded in `detail.disagreeing_readers`; no markers → the 2.2 precedence untouched under `fitted_no_markers`; a new `Q.SYSTEM_KEY` check (SYSTEM, before `key_signature`, reads rows not verdicts) abstains a lone dissenter `disagrees_with_system` and EXPORT carries (no `<key>` written → MusicXML carries). Transposition via `key_consensus.resolve_label` + `NO_SIGNATURE_CONVENTION` [C81]. Controls 54/54, 331/331, 691/691; `check` 258 → 257. **Engraved 10/18 → 18/18 parts, key changes 2 → 0, per-staff 50/0/0. Breitkopf 11/14 → 14/14, changes 265 → 212, wrong 224 → 127.** ⚠️ **Litolff 11/12 → 6/12 parts opening right** (per-staff wrong 37 → 33, so the reader is not worse per staff): on the merging plate the detector under-counts flats and a PART takes its key from its FIRST system — an export choice, not a reading; and `system_key` corroborates the wrong −1 there because Flute/Oboe/Clarinet misread TOGETHER (§10: two witnesses off one raster). Gate '0 key changes' met by neither arm (105 → 113). → 2.9b. ⚠️ The acceptance engraved record was RE-GATHERED (`c5e8f809`, manifest md5 `23c01008…`): the old one was dirty, pre-identity, no `Q.INPUT_DOMAIN`, so 2.2's tier never fired on it. `Log.subjects` was quadratic (Breitkopf control >38 min) — cached. Breitkopf crop corrects a truth file: `Pk.` prints no key where the dossier says −3. 4 system-header crops for Sean in `out/print/`. Amendment record:  Traced on the engraved acceptance record: three `keysig_marker` rows on EVERY staff, `fitted` −1 on every treble staff from a locator fit saying one accidental; the record has no `Q.INPUT_DOMAIN` row so 2.2's engraved rule never fired; a majority on that page is 8 vs 8. Original framing: — CLAUDE.md §10: a key change is printed at one bar on every staff of the system, so staves of one system reading different concert keys is a reading error, not music. Measured 2026-09-23: the engraved acceptance page writes ONE flat on 9 of 18 parts where the truth is three on every non-transposing part (2.2's 47/50 was a different page range); Litolff writes key CHANGES at bars 17, 33, 48, 64, 82 in a movement that has none (Viola +7 at m64, Violin II +1 at m48). Rule: normalise each staff's reading to concert by its instrument's transposition (`instruments.py` transposition conventions; a staff whose instrument is unknown votes at face value), take the system majority, write each staff the majority key re-transposed; a tie ABSTAINS and carries the last decided key; a change is written only where the MAJORITY changes at the same bar. ADJUDICATE change (`header.py` `key_signature`), priceable by `readjudicate` on the saved records. Gate: engraved page 18/18 against the truth; Litolff and Breitkopf count pages: one concert key per system on every staff, 0 key changes not printed on the plate (Sean checks the plate: one crop per system header). Keep the per-staff reading as a recorded dissent, never silently overwritten. |
| 2.9b | A staff's key is checked against the DOCUMENT's majority concert key, then the part's cross-system majority; a change is written only where a majority of staves change at the same bar (Sean, 2026-09-23, on the four header crops: *"all 4 of those crops are pieces with 3 flats and the staffs that have fewer flats are transposing"*) | **in flight** (`claude/part-key-majority-2.9b`). Sean's truth scored against the crops: **20 of 46 staves wrong** (`out/print/ADJUDICATION-sean-2026-09-23.json`), the wrong values scattered (+2, −4, +1) and the right one identical on 26 — the document is one key and the reader treats every staff as a fresh question. Gate: all 46 right or carried-right; engraved 18/18, Breitkopf 14/14 kept; Litolff ≥ 11/12; key changes Litolff 0 / Brahms ≤ 2 / engraved 0. Earlier framing: 2.9's Litolff regression is this: the reader is right on more staves (37 → 33 wrong) and the file is wrong on more parts (11/12 → 6/12) because a part opens with its first system's reading and the first system under-counts on the merging plate. Where a system's decided key for the part disagrees with the part's cross-system majority, the file carries the majority and the dissent is recorded (`disagrees_with_part`); a key CHANGE is written only where the majority CHANGES at the same bar on the part's other staves too. Priceable by `readjudicate`/export on the three records. Gate: Litolff ≥ 11/12 parts opening right AND 0 key changes not printed (the plate prints none in Beethoven 5 mvt 1; Brahms 1 mvt 1 prints 2), no loss on engraved 18/18 or Breitkopf 14/14; `<note>` unchanged. |
| 2.10 | A staff whose clef is unread and whose INSTRUMENT is decided takes a clef from the same part on OTHER SYSTEMS, else the instrument's conventional header clef — GAPS ONLY, in INFER, labelled (Sean, 2026-09-23: *"viola staff with unreadable clef reads as alto and it should check other systems if the alto clef can be found"*) | **BUILT AND MERGED 2026-09-23** (`claude/clef-gap-2.10` `964875a1`; `benchmarks/omr-clef-gap-2026-09/`). `inferences.fill_clef_gap`, flag `OMR_CLEF_GAP` default ON (deny-list), registered AFTER the slot-index rule (7 of 9 staves get their slot from it). Identity from the SLOT, not `Q.INSTRUMENT` (which abstains on all 9 — no margin label). The harness already admitted ABSTAINED priors; first rule to use it. Ordering solved by `evaluate.run_over`: a second pass, same DOWNHILL loop body, firing only where a rule's cause (or a declared `reads_beyond_cause`) is a value INFER just wrote; `restate_pitch` now never overturns a standing pitch (the arm found an `AlreadyAdjudicated` on `glyph/2/1/9/6/2`). Litolff: 9 abstained clefs → 9 filled by tier (2), 0 by tier (3), **0 DECIDED changed**; `no_pitch` 807 → 552; events 10,415 → 10,646; **gate met: `staff/3/0/9` writes 37 of 48**, `no_pitch` 40 → 0 on page 3, every other staff row-identical. Brahms/engraved: 0 abstained, unchanged. Tier (3) has reach ZERO on all three documents — convention C89 ASSERTED with that zero in its own field. Fast tier 2,861; `check` 254 (one source-text assertion rewritten behaviourally). ⚠️ After 2.10 the residual `no_pitch` population is the NARROWED clef (596 of 601) — a different item. ⚠️ Merged AFTER 2.11, which READS 8 of those 9 by geometry, so on today's tree 2.10's live reach is the remainder; **PRINT-ADJUDICATED BY SEAN 2026-09-23: 8 of 8 correct** (7 alto, p14 staff 11 bass; `out/print/ADJUDICATION-sean-2026-09-23.json`); the 9th, `staff/15/0/9`, has no crop — REFUSED by the frame control at contrast 7.14 (the record's geometry and the render disagree there). `OMR_CLEF_GAP` moves from `research` to `promote` at the next flag triage. Original: the Viola funnel (`benchmarks/omr-notehead-funnel-2026-09/`): 48 heads boxed, 0 written, because `clef` ABSTAINED `no_candidates` and `restate_pitch` had nothing. Evidence order, each speaking only into a gap and never overruling a READ clef: (1) this staff's own clef verdict; (2) the clef DECIDED on the same part (same `slot_index`/instrument) on other systems of the document — majority of those reads, a split ABSTAINS; (3) the instrument's conventional header clef from `instruments.py` (Viola → alto). Rule lives in INFER (`inferences.py`), target `Q.CLEF`, `sideways` for (2), with its own default-ON flag per the per-rule-gate convention. ⚠️ INFER runs AFTER EVALUATE, so an inferred clef must still reach `restate_pitch`: establish how (a bounded second EVALUATE pass over consequences of inferred values, with every derived pitch carrying the inferred label) — never by moving the clef guess into ADJUDICATE. Gate: Litolff p3 Viola system 0 writes ≥ 37 of 48 (the funnel's own estimate) with `no_pitch` 40 → 0 on the page; 0 READ clefs changed anywhere on the three documents; every inferred clef named by staff in the census; crops of every inferred-clef staff header for Sean. Priceable on the saved records (INFER/EVALUATE only). |
| 2.11 | The geometric clef read is not vetoed by a notehead box on clef-sized ink at the header (Sean, 2026-09-23: *"the clef box is too small — the size of clefs is consistent to the staff and the edge of the first measure on the system, that should be helpful geometrically"*) | **BUILT AND MERGED 2026-09-23** (`claude/clef-geometry-2.11` `24d26b8d`; `benchmarks/omr-clef-geometry-2026-09/`), **gate met on Litolff p3, FAILED on one Breitkopf staff, awaiting Sean's crop**. Height band MEASURED from 85 successful C-clef reads (3.36–4.63 spaces, median 4.32; shipped 3.3–4.7; `clefF` detector boxes sit at 3.5 INSIDE the band — only the F-clef dot veto separates them); Sean's x-check measured (cluster within −60..+3 px of siblings) and not built because it separates zero cases. Reach priced off the records first: Litolff 47 `occupied` abstentions on 27 staves, 8 newly readable (six are one plate's Viola, staff 9, eight times); Breitkopf 0 predicted. Change: `clef_locator._overridable_occupancy` (four conditions, the fourth added after the Brahms measurement: a detector CLEF box on the same ink refuses the override), `gather._occupied_notehead_rows`, refusal `is_a_clef` in `notehead_precision`; convention C88. Tests RED first 13/16; fast tier 2,842; `check` 255. Three real one-page re-gathers: **Litolff p3 — `staff/3/0/9` ABSTAINED → DECIDED alto, 0 of 19 READ clefs changed, `no_pitch` 40 → 0, heads written 0 → 35 of 48, the two clef boxes refused `is_a_clef`**; Brahms p1 inert (0 occupied, byte-identical, a control); **Brahms p6 — `staff/6/0/9` ABSTAINED → alto (the same fault on a second publisher) BUT `staff/6/1/3` DECIDED treble → DECIDED tenor: one READ clef changed.** The base's treble rested on one `clefG` box at `Q.CLEF_POSITION` 19.36 half-steps, ~5.7 spaces BELOW the staff (real treble clefs on that page read 4.05–4.14) — a neighbour's clef in the padding — and the crop shows a C clef printed; so the change looks like a correction, but WHICH C clef is not adjudicated and the gate is reported failed. **ADJUDICATED BY SEAN 2026-09-23, 3 of 3: Litolff Viola = alto ✓, Brahms `staff/6/0/9` = alto ✓, Brahms `staff/6/1/3` = TENOR ✓ — the changed clef was a correction of a wrong base reading, so the gate stands at 0 READ clefs wrongly changed** (`out/print/ADJUDICATION-sean-2026-09-23.json`). ⚠️ Second finding handed on: `Q.CLEF_POSITION` is gathered and `adjudicate_clef` does not read it, so an off-staff clef box supports a clef unopposed — 2.11b. Original: on Litolff p3 `staff/3/0/9` the CV locator FOUND a cluster h=4.5 spaces, w=2.1–3.4, centred on the staff at the header x (an alto clef's geometry) and abstained `occupied` because two `noteheadBlackOnLine` boxes (conf 0.64, 0.36) sat on that ink — `_occupied_boxes` lets any detector box veto the read (`gather.py:2104`, `clef_locator.py:824`). Rule: a cluster at the header (between the system's left edge and the first barline / key signature, at the x the system's other staves' clefs share) whose height in staff spaces matches a clef family (alto ≈ 4–4.5 on the staff; bass ≈ 3.5–4 with dots; treble ≈ 7–8 extending above and below) is READ as that clef by geometry, and a notehead-class box on it is refused `is_a_clef` (a 2.4a-style notehead refusal — a notehead is ~1 space tall and 1.3 wide). The occupied veto stays for boxes of other classes. Priceable off the records FIRST: every `clef_located` `occupied` abstention carries its clusters' w/h in spaces and the detector boxes that occupied them — count, on both whole-movement records, how many carry clef geometry, which class occupied them, and what the same system's other staves read (a system's clefs share an x and a scale). Then the change (GATHER: needs two re-gathers to price; a `--pages 3` base-vs-arm on Litolff is the control). Gate: `staff/3/0/9` clef DECIDED alto by geometry; 0 READ clefs changed on the three documents; the two notehead boxes on the clef refused; crops of every newly-read clef for Sean. Where 2.10 and 2.11 both apply, 2.11's READ wins and 2.10 stays silent. |
| 2.11b | `adjudicate_clef` reads `Q.CLEF_POSITION` and discounts a clef box that sits off the staff (found by 2.11: a `clefG` 5.7 spaces below the staff decided `treble` unopposed on Brahms p6 `staff/6/1/3`) | todo — the value existed and nothing read it; a CONNECT. Gate: that staff's treble no longer rests on the off-staff box; 0 other READ clefs changed on the three documents; measured position bands per family from the records first. |
| 2.12 | SHAPE FROM THE CLASS, ROLE FROM THE GEOMETRY — the audit, then one item per family (Sean, 2026-09-23, DECISIONS) | **AUDIT DONE AND MERGED 2026-09-23** (`claude/shape-role-audit-2.12` `a24db150`; `benchmarks/omr-shape-role-2026-09/FINDINGS.md`, `probe/role_disagreement.py --all`). Reach = boxes whose role-half the record's own geometry contradicts, three records: tie/slur 7,549 (already computed, refusal measured); OnLine/InSpace 6,389 (nothing reads it); artic/fermata Above/Below 1,134; two spellings of one head both surviving NMS 859 (no adjudicator can repair); **key vs accidental 811 (a hard GATHER filter)**; dot vs staccato 672; restWhole/restHalf 567; timeSig 106; flag Up/Down 48 (value exists, unread); head-on-clef 22 (fixed by 2.11). Controls: the engraved fixture returns 0 on six of ten families. **Recommendation: (a) remove the role filters family by family, decide roles in ADJUDICATE — 2,156 boxes across four lines, two of them needing NO re-gather; second step (e) collapse the role-twin channels before NMS (~30 lines in `yolo_detector.py`, no training) — reaches the 859 twins; (b) canonicalise-at-the-reader is right as a derived check, wrong as a refactor (49 `smufl_name` sites in 26 staged functions); (c) ink-first is where this points and is not affordable yet: 51.5% of Litolff and 48.2% of Breitkopf INK COMPONENTS are covered by NO BOX AT ALL and only 4–7.5% carry more than one class; (d) a shape-only detector is refused (round 5: every fine-tune deletes classes).** Items in reach order below. ⚠️ Two records are dirty-tree gathers — a census, not a baseline. Original:  Every place GATHER (or a reader it calls) filters or branches on the ROLE-half of a detector class — `key*` vs `accidental*`, `*OnLine` vs `*InSpace`, `rest*` vs a shape, `clef*` vs a cluster, `dynamic*` letters, `tuplet`/`numeral`, `augmentationDot` vs `articStaccato` — listed with: the function, the two claims the class conflates, the decision that should decide the role from geometry, and the REACH on the three records (boxes whose role-half disagrees with their geometry). Output: a table in `benchmarks/omr-shape-role-2026-09/FINDINGS.md` and one proposed roadmap line per family in reach order. Also sized, not built: the ink-first record Sean describes — `Q.INK` components as subjects, boxes as readings attached to ink, several candidate labels per box (the detector's per-class scores before NMS; CV locators) — what the detector call would have to return, what `record` would have to hold, and which stages already read `Q.INK` (none, by design). Three fixed today by hand and to be cited as the pattern: `_KEYSIG_CLASSES` (2.9b, in flight), the ownership twin test (2.6), the clef `occupied` veto (2.11). |
| 2.12a | The printed accidental's ROLE (key vs in-bar) comes from the header window, not its class | **in flight inside 2.9b** (`claude/part-key-majority-2.9b`) — 811 boxes; RED: 503 Breitkopf + 115 Litolff header `accidentalFlat` boxes produce no marker row today. Two re-gathers to price. |
| 2.12b | A rest's VALUE comes from the line it hangs on, not `restWhole`/`restHalf` | **BUILT, MEASURED AND MERGED 2026-09-23, not print-adjudicated** (`claude/shape-role-b-e-2.12` `8edeb4e1`; `benchmarks/omr-shape-role-2026-09/FINDINGS.md` §2.12b, `compare_b_e.py`). `rhythm._rest_ruling` measures the slot: class agreed → DECIDED; lands on the OTHER convention → NARROWED over both (never flipped — C17's bowing-plate exception); lands on NEITHER → ABSTAINED `rest_stands_where_no_rest_hangs`. Litolff 1,342 agreed / 102 narrowed / 198 abstained; Breitkopf 1,837 / 32 / 235; engraved 216 / 0 / 0 (the control). Refusing 563 rest values let **141 bars start adding up and stopped none** (2.8 held-out 1,874 → 1,771 Litolff, 4,565 → 4,525 Brahms); `<note>` 8,605 → 8,696 and 7,822 → 7,872; census balanced. Finding under `lands_on_neither`: 276 of 433 stand OUTSIDE the staff they are filed on (Breitkopf 186 at step −7.3 — the next staff down, via the cell pad) — an ownership question, recorded as `detail.outside_its_own_staff`; only 157 are a rest question. 31 crops for Sean in `out/print/` (21 neither, 10 overruled; 5 refused by the frame control). ⚠️ `Q.REST_POSITION` files 0 rows (`OMR_FAMILY_POSITIONS` OFF) and `inventory._producers` scans only `gather.py`'s AST — a derived-check blind spot, its own item. Original: 567 boxes, **no re-gather** (positions on the record); 134 land on the other convention, **433 land on neither** — a separate finding; crops for Sean first. |
| 2.12c | A small filled dot's role (augmentation dot vs staccato) comes from where it sits | todo — 672; two re-gathers; RED: 43 + 9 staccatos sitting where a dot sits. |
| 2.12d | A mid-staff `timeSig*` states a meter only where the system agrees | todo — 106; one re-gather; only makes the reader quieter. |
| 2.12e | A flag's stem direction is the STEM's, not the class suffix | **BUILT AND MERGED 2026-09-23** (same branch). 2,685 of 2,996 attached flags take their direction from the DECIDED `stem_direction`; 33 disagreements (1.2%) recorded; 0 durations move (engraved: 4 of 4, file identical). ⚠️ The audit's '2,676 unjoinable flags' is **205** on the adjudicator's own join (the audit's `f_flag_stem` joined only single-flag single-direction cells); 173 of the 205 are `stem_direction` ABSTAINING (`no_stem`, `stems_disagree`), not a missing join — do not open a flag-join item on the audit's number. Original: 48; **no re-gather**, a pure CONNECT; the finding underneath: 2,676 flags are unjoinable to a stem today. |
| 2.12f | An articulation's side (above/below) is measured, not read off the suffix | todo — 1,134; one re-gather; the fermata half stays record-only. |
| 2.12g | Collapse the role-twin detector channels before NMS (`*OnLine`/`*InSpace`, `key*`/`accidental*`) | todo — 859 visible twins plus the invisible half; ~30 lines in `yolo_detector.py`, no training; four re-gathers; gate: the notehead count must not FALL. Do AFTER the (a) lines, since it changes the detection set every one of them is measured against. |

Gate: > 80% of gathered noteheads reach the file on both scans; all parts
named; second count lower than the first.

## Phase 3 — The staged pipeline becomes the product

| item | what | status |
|---|---|---|
| 3.1 | LilyPond exporter for staged, reusing the `_lily_*` renderers | **done** (`claude/staged-lilypond-3.1` @ `bdd3896f`, off `origin/main` `ea4e45a9`). `tools/omr/staged/lilypond.py:to_lilypond` calls `export.build()`/`_pair_arcs`/`_place_wedges`/`_events`/`_voice_split` over a fresh `Record` — the SAME functions `to_musicxml` calls — then renders with the legacy exporter's own PURE renderers (`_lily_event`, `_lily_measure`, `_lily_measure_rest`, `_lily_key_name`, `_clef_to_lily`, `_lily_wedge_plan`); `tools/omr/export.py` untouched (`git diff` empty). ⚠️ ONE STRUCTURAL DEPARTURE FROM LEGACY, NAMED IN THE MODULE DOCSTRING: legacy's own `to_lilypond` emits one `\new Staff` per (page, system, staff), so a hairpin or slur crossing a system break is dropped by construction; this emits one `\new Staff` per PART (spanning every system, matching MusicXML's `<part>`) and gets the cross-system merge for free from the same `_pair_arcs`/`_place_wedges` passes, so that is NOT a documented drop here. Returns `(str, dict)`, not the bare `str` the roadmap line named — deviation stated up front, for the same reason `to_musicxml` returns a report: a documented drop must be counted, not merely true. **Measured on all three acceptance documents** (`benchmarks/omr-staged-lilypond-2026-09/compare_notes.py`): engraved Beethoven 5 mvt1 bars 1-24 (18 parts, 358 notes), Litolff Beethoven 5 mvt1 pp.1-4 scan (12 parts, 965 notes), Breitkopf Brahms 1 mvt1 pp.0-3 scan (14 parts, 2513 notes) — **44 of 44 parts across the three documents agree on their sorted pitch multiset with the MusicXML export of the same record; all three compile with `lilypond` at exit 0, 0 errors** (~1 s / ~4.7 s / ~8.8 s). Bar-check warnings (17 / 137 / 550) trace to a PRE-EXISTING bar-sum shortfall already present in the MusicXML file (confirmed by summing `<duration>` per bar independently), not to this exporter. "Unterminated tie" warnings (0 / 77 / 400) are LilyPond's pitch-matching tie resolution independently corroborating the tie-pairing defect CLAUDE.md's `omr-chord-tie-2026-09` already measured — a third instrument on a known finding, found by accident of compiling. ⚠️ ONE BUG FOUND AND FIXED measuring the engraved record: a fermata over a whole-bar measure rest was silently dropped (`_lily_measure_rest` builds a bare rest from the meter alone) — `_Bar.fermata` + `_render_bar` now append `\fermata`, restoring exact parity (77 = 77). ⚠️ ONE LIMITATION FOUND AND LEFT, because fixing it would be porting rather than reusing: `_lily_event`'s chord branch writes ONE articulation suffix per chord from `group_chords_in_measure`'s event-level DEDUPLICATED list, where `_mxl_note` writes one per notehead — a chord whose members share a mark collapses (Breitkopf: 112 MusicXML marks → 106 written), counted in `report["lilypond"]["articulation_marks_collapsed_per_chord"]`, zero on the other two documents. `--lilypond` wired into both `staged/__main__.py` and `staged/export.py`'s own CLI beside `--musicxml`, imported after the gather for the same reason; `reach.STAGE_OF_FILE` registers `lilypond.py` under `EXPORT` (it names no `Q.*` quantity itself). `tools/omr/tests/test_staged_lilypond.py` (17 tests, run RED against the unbuilt module first) reuses `test_staged_export.py`'s fixture helpers rather than duplicating them. `pytest -m "not slow"`: 2759 passed (up from 2731 on `ea4e45a9`, no regressions). `python3 -m tools.omr.staged.check`: 252 open findings, unchanged from `origin/main` alone — this lane adds zero. ⚠️ **What is NOT established**: exact per-VOICE note ORDER on the three real documents (the acceptance check compares sorted pitch multisets per part; order-exact agreement is checked only on the small synthetic fixtures in the test file, because reconstructing it reliably from real chord/tuplet-bearing LilyPond text via regex on 965-2513-note documents was judged not worth building this pass); no print was consulted; no OMR-NED (musicdiff scores MusicXML, not this exporter). `tools/omr/acceptance.py`'s roadmap-1.3 `musicxml2ly` stopgap was deliberately NOT rewired in this lane per the coordinator's instruction — the swap (call `to_lilypond` directly on the same in-memory record, drop the `musicxml2ly` round-trip, fold `report["lilypond"]` into `current.json`) is described for whoever schedules it. |
| 3.1b | Swap `tools/omr/acceptance.py`'s `musicxml2ly` stopgap for the native staged exporter (`staged/lilypond.py`); report bar-check and unterminated-tie counts per document as proxies | todo |
| 3.2 | Port the legacy-only list deliberately (weight routing first) | first half prepared (weight routing): `claude/prepare-1.1-ink-summary-routing`, `tools/omr/staged/weight_routing.py` imports `transcribe._route_weights` (never copies its picking logic); `--route-weights` / `--no-weight-routing` on the staged CLI. ⚠️ Deliberately does NOT make bare `--weights` omission trigger routing the way the legacy CLI's own convention does: omission has meant "no detector, every cell abstains" since before this staged CLI existed (cheap tests, weights-free cloud sessions rely on it), so routing needs an explicit request (`--route-weights`, or `--weights auto`) rather than mere absence — a considered deviation, not an oversight; reported for review. The domain classification runs once and is threaded into `gather_input_domain` (new `classification=`/`input_domain_classification=` parameters) so the routed weights and the recorded `Q.INPUT_DOMAIN` row can never disagree. Tested (16 tests, incl. real synthetic vector/raster PDFs through the real classifier, and the legacy soft-fallback-with-one-stderr-line case) — a GATHER-time decision, so no shared-record arm can price it; tonight's 1.1 gathers are its first real measurement. The rest of the legacy-only list (§0.2b's frozen table) is untouched. |
| 3.3 | Web app `omr_engine=staged`; page cap → job budget; default flips on acceptance parity | todo |
| 3.4 | Corrections as human evidence in the record; re-export; auto-accept over recorded verdicts | **BOTH HALVES BUILT AND MERGED 2026-09-23** (viewer `claude/stage-review-viewer-3.4b` `6a20f3ad`; evidence `claude/stage-review-evidence-3.4a` `4cca65dd`; `benchmarks/omr-stage-review-2026-09/FINDINGS.md` §A/§B). Start: `python3 -m tools.omr.staged.review.server --record <rec> --pdf <pdf> --staff staff/3/0/9` → `http://127.0.0.1:5075/?staff=staff/3/0/9`. Which stages SEE a human box, measured and pinned by a test: ADJUDICATE (`notehead_is_not_a_notehead` WEIGHS it via `human_not_a_symbol`; `stem_direction`, `notehead_is_a_whole_rest`, `duration` are handed it), EVALUATE (`restate_pitch`), EXPORT; NOT GATHER (never), NOT INFER (both duration rules need `Q.GLYPH_OWNER`, which a human box cannot have). A human box lacks every raster-derived mark (`Q.GLYPH_CONF`, band distance, ladder, beam, flag, dot) — ⚠️ so its duration is read from the head alone (a quarter), which is wrong for every beamed note and nothing on the row says so; pinned. Session test on the Viola staff: the two clef-boxes deleted → refused `human_not_a_symbol`, census moved, **MusicXML byte-identical** — the corrections changed the accounting, not the music, because the lever was the clef (now 2.11/2.10). A stance reaches no stage by design and is reported with the verdict's reason, decider and `checked_by`. The rerun's empty-sidecar control FAILED on today's tree by exactly 2.3's 69 inferences (the record predates the `OMR_INFER` flip) and passed with `OMR_INFER=0`. ⚠️ `staged.check` 254 → **258**: the two human quantities have a producer OUTSIDE the pipeline and `inventory`/`gather_coverage`/`reach` have no way to say so — N went UP, against §4d; **3.4b-check** opened: give the derived checks a declared out-of-pipeline producer so a human reader is WIRED, not explained. **Gate (a pass by Sean on the Viola staff producing a feedback file with at least one of his rows in a re-decided verdict's basis): TODO — Sean's next action.** Design:  *"a UI where it pulls up a staff on a system and shows me everything it is gathering — I give feedback by redrawing, deleting or adding boxes — then the next stage, you tell me what the stage is doing with that information and I give feedback on what is working and what isn't — each stage at a time, following the trail of information and decisions with human feedback."* Purpose, his words: *"I mainly want this information so that we can then use it to determine how to refine the build, rules and decisions — structurally. All of the info would be given to you to turn into fixes."* Corrections are WITNESSES (observations with `reader: sean`, appended, never mutating a machine row) that the stages RE-DECIDE with; a disagreement with a verdict is filed against the verdict id, never applied. First slice on Litolff p3 `staff/3/0/9` (Viola). Two lanes on one contract, the review-actions sidecar (`tools/omr/staged/review/`): (A) `human_evidence.py` ingests the sidecar into a record via `record_io`, `rerun.py` re-runs ADJUDICATE→EXPORT off the saved record and diffs verdicts, `feedback.py` writes the session-readable feedback file (every human row, the verdicts whose basis names it, every disagreement with the verdict's reason and rows); (B) `server.py` + static: pick a staff-system ordered by heads lost, GATHER view on the 600-dpi crop (staff lines, cells, detector boxes by class, ink), add/delete/redraw → sidecar; ADJUDICATE / EVALUATE / INFER / EXPORT views per glyph with reason + rows used, agree/disagree → sidecar; a re-run button calls (A). Gate: on the Viola staff, one full pass by Sean yields a feedback file a session can read, and at least one of his rows appears in a re-decided verdict's basis (a witness that changed nothing is reported, not hidden). |

Gate: a whole movement through the web app on staged to MusicXML + PDF; an
accepted correction changes the re-export and the record names the decider.

## Phase 4 — Import from IMSLP

| item | what | status |
|---|---|---|
| 4.1 | `reengrave import <work>`: rank, one browser click at the gate, ingest, run, export, render | todo |
| 4.2 | `Kind.MOVEMENT`; a whole work exports one file per movement | todo |
| 4.3 | Budget printed before the run; unattended mode owns its processes | todo |

Gate: Beethoven 5, four movements, four MusicXML + four PDFs, page ranges right.

## Phase 5 — Clean (ongoing)

Cleanup count every two weeks on the fixed pages; corrections filed as
evidence; auto-accept rules from recorded verdicts. Done for a publisher when
Sean would rather fix than re-enter.
