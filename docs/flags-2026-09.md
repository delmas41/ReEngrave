# Flags — the triage of 2026-09-22 (roadmap 0.2)

60 `OMR_*` environment flags were read in the tree on 2026-09-22 (39 on the
legacy path only, 14 staged only, 1 both). Sean approved the triage; the
verdicts are the manager's and are recorded in `docs/DECISIONS.md`.

Four verdicts:

- **promote** — the behaviour stays, the flag is removed. Where an A/B arm
  may still need the off arm before the acceptance harness exists, removal
  is scheduled at the Phase 1 gate; until then the flag stays with its
  current default and its measured direction convention.
- **delete** — measured and refused, or superseded: the code path goes, the
  benchmark `FINDINGS.md` stays.
- **research** — a producer nothing reads, or an unpriced experiment. Moves
  behind the single umbrella `OMR_RESEARCH=<name>[,<name>]` (roadmap 0.2b,
  a lane), may not be read by the product path, and carries a review item.
- **frozen** — legacy path only; frozen with it (DECISIONS 2026-09-22); may
  not be read from `tools/omr/staged/`.

The product path reads at most the 13 flags in §3 (plus `OMR_ENGRAVED_KEYSIG` until its promotion lands).

---

## 1. Staged and shared flags — verdicts

| flag | default | what it gates | evidence | verdict |
|---|---|---|---|---|
| `OMR_SLOT_FAMILY_BLOCK` | ON | INFER rule: unnamed trailing block placed on the reference's family run | 25/25 against print, 0 grafts; off arm used 09-21/22 for the flip measurement | **promote** — remove at Phase 1 gate |
| `OMR_HOLD_OUT_UNIDENTIFIED` | ON | EXPORT: an unnamed staff is held out and counted | never once run OFF since it shipped | **promote** — remove now |
| `OMR_METER_CARRY` | ON | a system that read no meter takes the last read one as a candidate the bars weigh | measured both publishers; Sean's call 09-15 | **promote** — remove at Phase 1 gate |
| `OMR_METER_FROM_BARS` | ON | bars name a LENGTH; the form is borrowed only where a read one matches | with the carry | **promote** — remove at Phase 1 gate |
| `OMR_METER_SEGMENTS` | ON | EXPORT reads the meter in force per bar | off arm last used 09-09 | **promote** — remove now |
| `OMR_WHOLE_REST_INK` | ON | EXPORT refuses a pitched note where the ink is a whole rest (the one rule that deletes) | 25/25 Litolff; 1/12 for the right reason on Breitkopf; outcome good both | **promote pending the first count** (roadmap 1.4); if the count charges it, revisit |
| `OMR_DIRECTION_TEXT_SCAN_GATE` | OFF | STAGED: skip the direction-word reader on a page proved to be a scan (267 s/page for six words) | built, measured 0 of 36 engraved pages gated | **promote ON** at roadmap 1.2, then remove |
| `OMR_DIRECTION_TEXT` | ON (both paths) | the direction-word OCR reader | 144 engraved edits; cost on scans handled by the gate above | **product flag** (§3) |
| `OMR_INFER` | OFF | the two INFER duration rules | never print-checked | **product flag until roadmap 2.3** decides promote or delete |
| `OMR_INK` | ON | `Q.INK` producer, one row per ink component, 115 MB/page | read by `trace --empty-claims` only | **research** — stays ON until roadmap 1.1 persists the summary instead of rows; review at 1.1 |
| `OMR_DOCUMENT_IDENTITY` | **ON** | two DOCUMENT producers: `Q.DOCUMENT_IDENTITY` (publisher, **year**, **plate**, **has_text_layer**, work, `image_type`) from the committed catalog, and `Q.INPUT_DOMAIN`, the MEASURED scanned-vs-engraved verdict off the PDF's own container (`source_kind: "container"`) | flipped ON 2026-09-22 on Sean's instruction that the printing be gathered in the first stage; `Q.INPUT_DOMAIN` is read by `adjudicate_key_signature`, `Q.DOCUMENT_IDENTITY` still by nothing | **promote the domain half** (it has a consumer); **research** for the catalog half — re-triage when something reads the publisher/year/plate |
| `OMR_ENGRAVED_KEYSIG` | **ON** | ADJUDICATE: on a document MEASURED engraved, `adjudicate_key_signature` reads the template BEFORE the locator; one-sided, so a scan, a classifier abstention, a record with no identity row and the flag off all keep the shipped precedence | key signature right **26 → 47** of 50 and wrong **24 → 3**; bars exact **313 → 322** of 360, 4 parts better 0 worse; of 29 quantities exactly 2 move; scan fall-through proved a no-op on a real gather with a forged-engraved control. ⚠️ n = 1 engraved document, 1 renderer, **no print consulted** (`benchmarks/omr-document-identity-2026-09/FINDINGS.md`) | **promote pending a second engraved document** (roadmap 2.2); the 10 held editions the container reader measures engraved are named in that benchmark's `out/library-domains.json` |
| `OMR_FAMILY_POSITIONS` | OFF | a position row per mark for eleven families | producer only | **research**; review at roadmap 2.4c |
| `OMR_METER_TEMPLATE_AT_BAR` | OFF | template reader at candidate mid-staff bar heads, 3-staff quorum | unpriced GATHER change; no page in reach prints a mid-staff change | **research**; review when a fixture with a real mid-staff change exists (`beethoven-sym1-mvt1` m13 is the cheapest) |
| `OMR_VERTICAL_RUNS` | OFF | every vertical candidate the stem opening produced, accepted or refused, 1 MB/page | on arm never used outside its own FINDINGS; the page-frame reader (`vertical_runs_page.py`) answers the question it was built for | **research, candidate for delete**; review at roadmap 2.4a |
| `OMR_SLOT_CONSTRAINTS` | ON | ADJUDICATE: the identity constraint channels (score order, clef glyph, bracket block) entail a slot where the evidence forces one | landed 2026-09-22 on `claude/instrument-identification-channels-8dcaa2`; measured on Litolff: moves 13 placements from INFER to ADJUDICATE, `staff_not_identified` 141 → 141 (count unchanged) | **promote** — remove at Phase 1 gate; its off arm is the one-word reversal of a default-ON change until then |
| `OMR_ADJUDICATE` | OFF | `staged/pipeline.py` mode switch: `0` legacy, `shadow`, `1` | `shadow`/`1` have never been run; the staged CLI is the entry point and roadmap 3.3 adds `omr_engine=staged` | **delete** |

## 2. Legacy-only flags — frozen

Frozen with the legacy path. Defaults as coded on 2026-09-22. None may be
read from `tools/omr/staged/`; a staged equivalent is a new decision with a
roadmap item, never a re-read of one of these.

| flag | default |
|---|---|
| `OMR_ROSTER_LABELS` | off |
| `OMR_LEFT_EDGE_SPLIT` | ON |
| `OMR_REFERENCE_MOST_LABELLED` | off |
| `OMR_CHOIR_GROUPING` | ON |
| `OMR_SLOT_GROUP_MAP` | "" |
| `OMR_BRACKET_COLUMNS` | ON |
| `OMR_SPAN_REFERENCE_FIT` | "" |
| `OMR_MOVEMENT_REFERENCE` | ON |
| `OMR_LINEUP_SWAP_SPLIT` | off |
| `OMR_TAIL_RULE` | exact |
| `OMR_PARTIAL_DYNAMICS` | off |
| `OMR_ARC_ATTRIBUTION` | move |
| `OMR_ARC_RECLASS` | off |
| `OMR_SLOT_STITCH` | ON |
| `OMR_CONDENSED_PARTS` | off |
| `OMR_PHASE26_FIXES` | all |
| `OMR_PHASE28_FIX_TEXT_GATE` | on |
| `OMR_ROSTER` | ON |
| `OMR_ROSTER_RANGE_VETO` | off |
| `OMR_CLEF_WEIGHTS` | none |
| `OMR_CONTEST_DUMP` | off |
| `OMR_CV_HAIRPINS` | off |
| `OMR_INSTRUMENT_CLEF_DEFAULT` | off |
| `OMR_KEYSIG_CORROBORATION` | ON |
| `OMR_SCORE_LANGUAGE` | off |
| `OMR_ABSENT_INSTRUMENT_VETO` | ON |
| `OMR_ROSTER_SCORE_ORDER_VETO` | off |
| `OMR_LABEL_MERGE_QUALITY` | ON |
| `OMR_ROSTER_CLEF` | off |
| `OMR_ONE_LINE_STAVES` | off |
| `OMR_STEM_NOTEHEAD_GATE` | off |
| `OMR_STEM_STROKE` | off |
| `OMR_EVAL_INDENT_MM` | "" |

Three of these are the shared structural readers (`OMR_LEFT_EDGE_SPLIT`,
`OMR_CHOIR_GROUPING`, `OMR_BRACKET_COLUMNS`) which the staged path also runs
through `system_grouping.py`; they are ON, measured, and promoted in place —
remove the flags when the legacy freeze is enforced by a test (roadmap 0.2b).
`OMR_STEM_STROKE` and `OMR_STEM_NOTEHEAD_GATE` live in `line_detection.py`,
which both paths call; both are OFF and stay OFF; roadmap 2.4a re-decides
them on the staged path with its own item.

## 3. Product flags — the thirteen the product path may read

| flag | default | purpose |
|---|---|---|
| `OMR_WEIGHTS_PATH` | unset | pin one weights file; disables routing |
| `OMR_WEIGHT_ROUTING` | ON | pick weights by input domain (staged gets this at roadmap 3.2) |
| `OMR_ENGRAVED_WEIGHTS` | the imgsz2048 checkpoint | the engraved-side weights |
| `OMR_MAX_PAGES` | 5 | web-app page cap (becomes a job budget at 3.3) |
| `OMR_CONF_THRESHOLD` | 0.25 | detector confidence |
| `OMR_IMGSZ` | 512 | detector image size |
| `OMR_DPI` | 300 web / 600 CLI | rasterisation |
| `OMR_DIRECTION_TEXT` | ON | the direction-word reader |
| `OMR_SURYA_KEEP_ALIVE` | off | attach to a resident Surya server |
| `OMR_SURYA_TIMEOUT_S` | 900 | Surya worker timeout |
| `OMR_WORK_ID` | unset | name the catalogued work for a PDF the library does not hold |
| `OMR_INFER` | OFF | the two INFER duration rules, until roadmap 2.3 |
| `OMR_RESEARCH` | unset | the umbrella for every `research` verdict above |

Everything else is gone, promoted, or frozen. A new flag needs a roadmap
item and a line here.

## 4. What this triage does NOT do

It moves no code today. Roadmap item **0.2b** (a lane) enforces it: remove
the `promote — now` flags, delete `OMR_ADJUDICATE`, introduce
`OMR_RESEARCH`, and add one derived test asserting that no module under
`tools/omr/staged/` reads a flag from §2 and that every `OMR_*` read in the
tree appears in this file. Until 0.2b lands, every flag behaves exactly as it
did on 2026-09-22.
