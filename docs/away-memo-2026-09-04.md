# Away memo — running decision brief for Sean (started 2026-09-04)

Maintained by the advisor session while Sean is away (a few days from
2026-09-04 evening). Standing policy in force: **nothing ships** — no default
flips, no scan-slot changes, no `--record` headline runs; every result lands
committed and pushed on its own branch. This file accumulates the decisions
waiting for Sean, newest first within each section, and is committed as it
grows.

---

## Decisions waiting for you

1. ~~Arcs graft ship?~~ **PATH CLOSED by round 7 — no decision needed.**
   Verified on `claude/scan-weights-round4-continue-074940` (2bfd005a,
   pushed; `benchmarks/omr-queue-arcs-2026-09/ROUND7_FINDINGS.md`). Two
   findings ended it: (a) your adjudication flipped the residue story —
   **260 of 268 teacher arc candidates were FAKES**, so the "58/61%
   unboxed" figures measured the teacher's hallucinations, not missing
   human labels; (b) frozen-head arc specialists are dead from both jaws
   (default LR = silent null, rows move 0.2–0.4%; aggressive LR fires
   6.7× without discriminating, in-sample precision 0.037 vs production's
   0.232) — frozen features simply don't separate real arcs from
   edge-bleed, and unfreezing is the measured class-deletion move. The
   specialist→graft recipe stays valid where features already separate
   (noteheads); arcs need a different sensor. REPLACEMENT (your approval,
   already running): a classical-CV arc-tracer R&D session, tested against
   the 260-fake gauntlet — which is the position-grammar direction
   exactly (an arc's identity and existence argued from anchors, with
   "matches nothing → drop" as the fake-filter). Findings land on its own
   branch.
2. **Condensation-floor reporting decision** (from ERROR_FORENSICS): an
   estimated **8,252–8,560 edits (28.4–29.4% of the pooled 29,082) are
   IRREDUCIBLE floor** — parts the printed page cannot express (condensation),
   charged as whole-staff/measure ops no recognition improvement can remove.
   Decision: keep quoting the raw pooled figure only, or report a
   floor-adjusted companion number alongside it (protocol/reporting choice,
   not code). Evidence: `benchmarks/omr-scan-e2e-2026-09/ERROR_FORENSICS_2026-09-04.md`
   (branch `claude/scan-error-forensics`, pushed).
3. ~~Choir flip + Bach re-admission~~ **DECIDED & SHIPPED by Sean
   (2026-09-05), re-stamp COMPLETE:** canonical scan-gate baseline is now
   **pooled 0.8303 / 35,046 over 11 rows** (composed defaults: tilt ON x
   choir ON, graft weights; `results-restamp-composed.json` + third
   WIDENED_BASELINE addendum). All ten non-Bach rows reproduce the tilt
   arm to the edit; Bach enters at 0.8110 — better than the pool average.
4. ~~Tilt fix~~ **DECIDED & SHIPPED by Sean (2026-09-04, mid-trip):**
   `OMR_CELL_LINE_TRACE` is default-ON. Deployed default-config scan
   baseline: **pooled 0.8345 / 28849** (10-row pool, graft weights).
   Evidence + blast radius: WIDENED_PRICING_2026-09-04.md; CLAUDE.md knob
   row + limitation bullet updated; flag tests pin the new default.
5. **Arc veto: enable the slur→tie HALF?** The export lane landed (merged to
   this branch, flag `OMR_ARC_RECLASS` default OFF). Verdicts: the ACCENTS
   gap was already closed by the 09-02 articulations merge — only prose was
   stale, now corrected, plus the missing LilyPond test. The veto itself:
   engraved +2 edits with the Mozart 41 tie channel corrected 9→1 vs truth 1;
   scan REFUSED as a whole (+130) — but the per-direction split is decisive:
   ALL damage is the tie→slur direction (pitch-error-inherited vetoes — your
   design note's "anchors before grammar" measured in the wild), while
   **slur→tie alone is edit-free and moves exported ties 420→462 of 805**.
   Decision: split the flag and default the slur→tie half on? Evidence:
   `benchmarks/omr-export-gaps-2026-09/FINDINGS.md`. Design refinement worth
   knowing: the same-pitch test must compare STAFF STEPS, not spelled
   pitches — tied heads across barlines don't restate accidentals (spelled
   comparison lost 21 engraved edits before the fix).
6. **Row tranche 2 — READY for your glance-and-confirm (~20 min total):**
   9 rows drafted on `claude/scan-gate-rows-2` (cd40bf83, pushed), chaining
   1–2 pages beyond every verified edition: Beethoven twins p3 (mm 49–82) +
   p4 (83–112), Brahms p3 (23–37) + p4 (38–58), Mahler p4 (17–23) + p5
   (24–31), Dvořák p7 (16–30, the counting row). Checklist:
   `ROW_VERIFICATION_CHECKLIST_2026-09-05.md` in that worktree's scan-e2e
   dir, easiest first, every number linked to a crop
   (`crops-2026-09-05/`, 33 PNGs, committed). Notables: Brahms p4 is the
   first window containing a REPEAT BARLINE (future repeat-export test
   material); 984073-p4 prints two systems with DIFFERENT lineups — the
   drafter's positional pairing was hand-corrected from margin crops with
   reference corroboration (Timpani's first note exactly m98), so verify
   the correction there, not the drafter; two pipeline miscounts were
   localized and recorded (a shared header sliver on Mahler p4 — the
   0.8706 lesson relived — and one gained barline on 575951-p4, pinned to
   mm 94–95 by the twin fingerprint).

## Landed while you were away

- **CLEF LANE COMPLETE (branch `claude/clef-string-staves`, head ebed31c9,
  pushed):** the diagnosis changed shape under verification. The
  constant-offset clef damage splits three ways: (A) header clefs misread
  as treble on label-NAMED staves — reachable, ~300 raw edits; (B)
  mid-staff spurious clef flips (the Vn2 case the brief mis-filed as a
  header default); (C) the BULK (~1,100+, both Beethoven p2 rows) on staves
  the plate never names at all — the Litolff p.2 margins print nothing at
  any string staff, and score-order naming is forbidden prior art, so no
  identity-gated lever can reach it (the real blocker is margin reach, as
  the clef-leverage memory predicted). Candidate delivered anyway:
  `OMR_INSTRUMENT_CLEF_DEFAULT` (default OFF), two identity-gated tiers +
  a key-spelling restatement fix, 50 unit tests, suite 1900 green; every
  firing on a verified site, byte-identical everywhere else incl. all 11
  engraved works. A/B (measured pre-composed-ship, vs 0.8387/29082): raw
  **−45**, plus **−254 condensed edits** the raw metric is provably blind
  to (musicdiff pairs pred-Viola with truth-Eb-Horn under the condensation
  floor). ⚠️ Decision guidance: HOLD the flip for now — its raw value is
  small, it needs a re-A/B on composed defaults before any flip, and its
  real significance is as evidence: adopting the condensed-column
  reporting protocol (your open decision #2) roughly QUADRUPLES this
  candidate's measured value on the same bytes. Also found: a `clef_source`
  provenance staleness bug (recorded, default-path, unfixed). Evidence:
  `benchmarks/omr-clef-string-staves-2026-09/FINDINGS.md`.


- **ERROR FORENSICS (2026-09-04, branch `claude/scan-error-forensics`,
  8d2e6b90):** the ranked-lever read of the 0.8387. Top levers: (1) **scan
  detection recall/precision incl. rests, ~4,000–7,000 edits, currently
  unowned** — the largest open cause now that head surgery is banked and
  rounds 5/6 + DSv2 closed every training route; rests alone cost ~2,211
  note-op edits, and 566 of 568 "wrong note head" edits in correctly-read
  bars are wrong REST values. (2) The condensation floor above. (3) **Clef on
  string staves, ~1,900–2,700 edits** — constant-offset damage with clean
  signatures (Viola read as treble on three rows and as bass on the low-res
  twin; a Violino II read as bass), now spun off as its own lane. Also
  measured: real segmentation faults are under ~300 pooled edits; `wrong
  note` on scans is NOT rhythm (duration ≈ 713 pooled — the engraved-era
  prior does not transfer); the Brahms p2 stitch refusal costs only ~24 edits
  today.
- **Record erratum fixed:** WIDENED_BASELINE's 575951-p2 footnote claimed a
  "true 16+16" split; the engraver's printed 17/34/49 (VERIFICATION.md) makes
  the truth **17+15**, so the pipeline's read there was exactly right, not a
  boundary slide. Corrected in place.

## Reconciliation ledger (for the merged-tree pass)

- **Duplicate test fix, resolved by rule:** the chip-spawned session pushed
  `1117c2db` on `claude/distracted-hugle-218685` — a 10-line version of the
  same `test_windows_accept_the_scan_benchmark_file` narrowing this branch
  already carries in fuller form (refusal assertion + single-edition
  coverage + cross-selection `same-as`). At reconciliation, THIS branch's
  version wins; the other branch can be archived unmerged.
- **origin/main has advanced in parallel:** PRs #12 (tilt-crosscheck) and
  #13 (objective-chatelet) merged there, plus labeling-audit commits this
  branch predates. The queued merged-tree trial is now a concrete task:
  reconcile `claude/score-labeling-training-system-iech0i` (three ships,
  the widened gate, the boundary events) with origin/main in ONE deliberate
  merge, measured on the merged tree before anything lands — the
  multi-session landing protocol applies.

## Lane status board

| lane | owner | status |
|---|---|---|
| arcs specialists → graft → gate | round-4 session | **CLOSED (round 7) — teacher arcs 260/268 fake; frozen features can't see arcs** |
| CV arc-tracer R&D (replacement) | separate session, Sean-approved | running against the 260-fake gauntlet |
| Bach choir grouping | agent, `claude/bach-choir-grouping` | **done — SHIPPED default-ON + Bach re-admitted (re-stamp in flight)** |
| export gaps: accents + arc veto | agent, `claude/export-accents-arcs` | **done, merged to this branch** |
| tilt pricing on widened gate | agent, `claude/tilt-pricing-widened` | **done — SHIPPED default-ON by Sean** |
| error forensics | agent, `claude/scan-error-forensics` | **done, pushed** |
| row tranche 2 (drafts + crops) | agent, `claude/scan-gate-rows-2` | **done, pushed** (9 rows, cd40bf83) |
| clef-on-string-staves lever | agent, `claude/clef-string-staves` | **done, pushed — candidate default-OFF; hold pending condensation decision** |
| merged-tree trial (all branches) | advisor session | queued behind the running lanes |
| grace-by-geometry | — | queued candidate |
